A data class that is used to hold the information of all the information retrieved from the loaded excel files.

The model structure crystallizes runtime after the modelbuilder has collated the worksheets of every excel file.


The "primary key" for each record in the model is stored in a json config file. come from a column in the excel file with column header "familyID" or similar