# Core Todo

- [x] Define `Prism.Process` input type: name the C# type that carries normalized images, Excel files, zip results, options, and cancellation into core.
  - Answer:
    Use a C# PODO named `PrismJobRequest`.

    `PrismJobRequest` is the structured job contract passed into `Prism.Process`. It represents one logical client-requested job after API, WPF, local path, stream, URL, multipart, and zip inputs have been structured into PRISM input records. `Prism.cs` acts as the outward-facing facade: it receives caller input, builds or completes the `PrismJobRequest`, generates PRISM's own internal `JobID` at the start of `.Process(...)`, passes structured job input onward for processing, receives structured output from the processing/exporter flow, and returns the processed result to the requester.

    Required fields:
    - `Guid JobID`: generated by `Prism.Process` at the start of processing. External caller-provided IDs must not become PRISM's internal job ID.
    - `string? ClientRequestToken`: optional caller-provided token. This is only echoed back to the requester so external systems can correlate the returned result with their original request.
    - `IReadOnlyList<InputImageRecord> ImageRecords`: image input records. `InputImageRecord.cs` is a PODO and carries image input metadata and normalized input references.
    - `IReadOnlyList<InputExcelFileRecord> ExcelRecords`: Excel input records. `InputExcelFileRecord.cs` is a PODO and carries Excel input metadata and input references.
    - `IReadOnlyList<InputZipFileRecord> ZipFileRecords`: zip input records. `InputZipFileRecord.cs` is a PODO and carries zip input metadata, extracted member information, and zip-level import state.
    - `PrismProcessingParameters PrismProcessingParameters`: per-job processing parameters, including output format, transform toggle, generation toggle, diagnostics settings, and `ReturnOriginalImages`.

    Processing rules:
    - `PrismJobRequest` is the contract between the outward-facing caller and the `Prism.cs` facade.
    - `Prism.cs` should read as simple orchestration code: structure input, generate the internal `JobID`, call pipeline, call explicit cleanup/error helpers, and return the result.
    - `Pipeline.cs` owns processing and disposal of pipeline resources. It receives structured input from `Prism.cs` and returns structured output through the exporter flow back to `Prism.cs`.
    - `Prism.cs` handles cleanup only by calling explicitly named helper classes or methods. Cleanup and error logic must live in dedicated classes such as `JobCleaner.cs`, `JobErrorHandling.cs`, or equivalent strategy/helper classes.
    - `PrismJobRequest` must not expose raw frontend upload objects, API-specific request types, WPF-specific objects, or platform-specific link objects.
    - There is no cancellation token and no cancellation path. Every accepted `Prism.Process` request is processed in the same way from start to natural completion.

    Validation rules:
    - `Prism.Process` creates the internal `JobID`; it does not trust or reuse caller-provided job IDs as PRISM job IDs.
    - `Prism.Process` rejects a job before pipeline execution if the job has no accepted image records, no accepted Excel records, missing `PrismProcessingParameters`, or invalid input record structure.
    - User-file failures already discovered during import, stream handling, URL resolution, or zip handling remain attached to the relevant input records so they can be included in the final manifest.
    - Processing is never cancelled by request option. Any accepted request is handled start to finish and ends as a completed job with OK and/or KO records.

- [ ] Define `Prism.Process` output type: name the C# type that returns output images, manifest data, KO details, and processing diagnostics.
  - Impact:
    - Project progress: High - The output type is the shared result contract for zip export, JSON export, API responses, and workbench previews.
    - Effect on other TODOs: Blocks - It controls manifest projection, output records, KO grouping, diagnostics, and client result handling.
  - Industry standard:
    Batch processors return a structured result envelope with summary counts, successful records, failed records, artifacts, diagnostics, and safe metadata for audit.
  - Recommended solution:
    Define a `PrismJobResult` containing output image records, batch manifest, KO records, diagnostics, export metadata, and original image data only when `PrismProcessingParameters.ReturnOriginalImages` is true.
  - Answer:

- [x] Define pipeline stage order: list the exact chronological stages from import normalization through export.
  - Answer:
    Use this chronological stage order for one PRISM job, from accepted input through returned output:

    1. `JobAccepted`
       `Prism.cs` accepts a `PrismJobRequest`, generates the internal `JobID`, validates that required input records and `PrismProcessingParameters` exist, and prepares job-scoped context.

    2. `ImportNormalize`
       Input records are normalized by Importer-owned logic. Local files, streams, multipart uploads, URLs, and zip members are converted into accepted image, Excel, and zip records. Supported images/documents become normalized JPG artifacts where applicable. User-file failures become KO records and stay attached to the job.

    3. `BuildExcelModel`
       Excel records are parsed into the internal deduplicated Excel model. Valid rows become product/family records. Invalid rows, invalid worksheets, and unusable Excel inputs become KO records without stopping valid work.

    4. `PrepareImageRecords`
       Normalized image records are prepared for matching. This includes stable source image state, original filename metadata, normalized artifact references, hashes or dimensions where available, and filename tokenization.

    5. `MatchImagesToProducts`
       Numeric, string, and image-label matching evidence is collected and scored against the internal Excel model. Each image receives a match decision, candidate evidence, tie state, threshold state, or KO state when no acceptable FamilyID match exists.

    6. `ClassifyImages`
       Matched images are classified into bounded traits needed by ordering and transformation, such as image type, orientation, human visibility, head visibility, border intersections, confidence values, and unknown states.

    7. `TransformImages`
       Images that are eligible for output are preprocessed, cropped, resized, centered, background-filled, cleaned up, or rejected as KO according to transform rules and `PrismProcessingParameters`.

    8. `GenerateImages`
       If generation is enabled, PRISM evaluates FamilyID image collections and creates supplemental generated images only when configured quality and source-image requirements are met. If generation is disabled, this stage is skipped but still considered part of the canonical order.

    9. `OrderAndRenameImages`
       All OK processed and generated images are grouped by FamilyID, ordered, assigned `_det` suffixes, checked for filename collisions, and assigned final output filenames.

    10. `BuildManifest`
        PRISM builds the canonical `BatchManifest` from the completed job state, including summary counts, OK records, KO records, source metadata, output metadata, processing parameters, and safe diagnostics.

    11. `ExportResults`
        `Exporter.cs` projects the canonical result into the requested output format. Zip output contains processed images and `manifest.json`; JSON output contains manifest/result data and image bytes according to export parameters, including original images only when `ReturnOriginalImages=true`.

    12. `CleanupJob`
        `Prism.cs` calls explicitly named cleanup helpers such as `JobCleaner.cs`. Cleanup removes job-scoped temporary files and releases job-scoped resources after export has been prepared.

    Stage rules:
    - The stage order is fixed for every accepted job.
    - Stages may internally process many items, but the stage sequence itself must remain chronological and predictable.
    - A stage can emit OK records, KO records, warnings, diagnostics, and progress events.
    - User-file KO records do not stop the job when valid work remains.
    - PRISM-owned failures such as missing config, missing model files, invalid internal settings, or exporter failure stop the job as `Failed`.
    - There is no cancellation stage. Accepted jobs run until natural completion.

- [ ] Define user-file failure policy: say which bad inputs become KO records and allow the batch to continue.
  - Impact:
    - Project progress: High - Bad user inputs are expected at scale and must not collapse otherwise healthy batches.
    - Effect on other TODOs: Blocks - It informs IO KO reasons, zip member handling, API errors, manifest rows, and workbench rejection states.
  - Industry standard:
    Large data pipelines route bad records to a dead-letter or KO path with reason codes while continuing healthy records unless correctness requires full-batch failure.
  - Recommended solution:
    Treat unsupported, corrupt, unreadable, oversized, unmatched, or untransformable user files as KO records when the batch can still produce valid outputs.
  - Answer:

- [ ] Define Prism-owned failure policy: say which missing configs, model files, or invalid internal settings stop the whole pipeline.
  - Impact:
    - Project progress: High - Internal configuration and model failures must fail fast to avoid invalid outputs.
    - Effect on other TODOs: Blocks - It affects config lifecycle, ONNX ownership, health checks, and resource initialization.
  - Industry standard:
    Pipeline-owned dependency failures are readiness or startup failures, not per-record errors, because retrying individual items cannot fix missing models or invalid configuration.
  - Recommended solution:
    Fail the whole job before processing if required config, model assets, schemas, or internal thresholds are missing or invalid.
  - Answer:

- [ ] Define Prism configuration loading lifecycle: say when `Prism_Config.json` and folder-local configs are loaded and validated.
  - Impact:
    - Project progress: High - Configuration lifecycle determines when limits, model paths, and thresholds become authoritative.
    - Effect on other TODOs: Unblocks - It feeds request validation, resource initialization, settings split, and health/config endpoints.
  - Industry standard:
    Batch services load and validate configuration before accepting jobs, snapshot effective config per job, and avoid reading mutable config mid-stage.
  - Recommended solution:
    Load central and folder-local configs during Prism startup or first use, validate them before job acceptance, and attach an effective config snapshot to each batch.
  - Answer:

- [ ] Define Prism resource initialization lifecycle: say when ONNX sessions, image processors, temp storage, and other heavy resources are prepared.
  - Impact:
    - Project progress: High - Heavy resource initialization controls latency, memory pressure, and operational readiness.
    - Effect on other TODOs: Unblocks - It drives ONNX session lifetime, health reporting, temp folder handling, and failure policy.
  - Industry standard:
    Large media systems initialize reusable expensive resources at application readiness and allocate job-scoped storage only when a batch starts.
  - Recommended solution:
    Prepare shared model sessions and processor pools at startup/readiness, then create per-batch temp storage and scoped buffers when `Process` begins.
  - Answer:

- [ ] Define Prism resource disposal lifecycle: say which resources are released after each batch and which live for the application lifetime.
  - Impact:
    - Project progress: High - Disposal policy prevents leaks during repeated large batches.
    - Effect on other TODOs: Unblocks - It affects temp cleanup, stream ownership, ONNX lifetime, and workbench direct invocation.
  - Industry standard:
    Production batch workers separate application-lifetime resources from job-lifetime artifacts and guarantee cleanup in success, KO, cancellation, and failure paths.
  - Recommended solution:
    Keep ONNX sessions and immutable config application-scoped, dispose image streams/temp folders/job buffers after export or failure, and expose deterministic disposal on Prism shutdown.
  - Answer:

- [ ] Define pipeline progress event shape: list the fields emitted by each stage so API and workbench can display progress.
  - Impact:
    - Project progress: High - Progress events are the common observability contract for API, web, and WPF.
    - Effect on other TODOs: Unblocks - It feeds API streaming, workbench subscription, progress visualization, and diagnostic snapshot display.
  - Industry standard:
    Long-running batch systems emit structured, monotonic progress events with stage, item identity, counts, severity, message, timestamp, and correlation metadata.
  - Recommended solution:
    Emit `PipelineProgressEvent` with job ID, stage, current item, completed count, total count, severity, message, timestamp, and optional snapshot reference.
  - Answer:

- [ ] Define settings kept in `Prism_Config.json`: list the global values that remain in the central Prism config file.
  - Impact:
    - Project progress: Medium - Central settings matter, but they should follow the config lifecycle and subsystem ownership decisions.
    - Effect on other TODOs: Influences - It affects API validation, image size limits, transform bounds, generation limits, and health/config responses.
  - Industry standard:
    Shared config files hold cross-cutting operational limits and defaults, while component-specific tuning stays close to the component that owns it.
  - Recommended solution:
    Keep batch limits, accepted global output bounds, request limits, temp storage policy, and feature flags in `Prism_Config.json`.
  - Answer:

- [ ] Define settings moved to folder-local configs: list the values that should live beside Excel, IO, image, matching, transform, and export code.
  - Impact:
    - Project progress: Medium - Local configs reduce central coupling once the global config boundary is set.
    - Effect on other TODOs: Influences - It affects Excel indicators, matcher thresholds, transform policies, zip layout, and model metadata.
  - Industry standard:
    Pipeline components keep domain-specific thresholds, vocabularies, schemas, and layout rules near their implementation so ownership and review are clear.
  - Recommended solution:
    Move Excel header rules, matcher weights, classification prompts, transform thresholds, zip layout, and export field mapping into folder-local configs.
  - Answer:
