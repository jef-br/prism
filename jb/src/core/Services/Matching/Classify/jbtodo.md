# Image Classification Todo

-------
- [ ] Define final ImageNGP taxonomy and feature combinations: list all possible ImageNGPs and the ImageFeature values required to derive each phenotype.
  - Answer: FROZEN: Taxonomy is captured in canonical files (ImageNGP.json, ImageRoles.json, imagePhenotypes.md, ImageFeatures.md). No reconciliation action needed at this time.
  - Superseded (T-4700, 2026-07-27): acted on for real, not just re-answered. 10 empty-body analyzer stubs were deleted, taking 22 stub-only features and 6 phenotypes down with them — those features could never be anything but UNKNOWN, and `PhenotypeRuleSet` never treats UNKNOWN as satisfying a `required` condition, so the affected phenotypes were mathematically unreachable, not merely unvalidated. One more feature (`type-of-shot`) was removed alongside them for the same reason despite never having even a stub producer — 23 features gone in total. Taxonomy is now 37 features / 20 phenotypes, all with a real producer. See `jb/docs/ImageNGP/HowToAddAPhenotype.md` for the reconciliation process going forward, and `Analyzers/jbtodo.md`'s "Removed" section for what was deleted.

-------
- [ ] Phenotype production validation: define the protocol and acceptance criteria required before phenotype assignment can be trusted in production.
  - Issue: The 20 phenotypes (26 before T-4700) in `imagePhenotypes.md` were defined from spec and taxonomy documentation without real-image testing. Production-quality assignment requires validation against a representative labeled image set. Currently most features are UNKNOWN (see per-feature Analyzer todos in `jb/src/core/Services/Matching/Analyzers/jbtodo.md`), so phenotype assignment is unreliable for any image where CLIP evidence is needed.
  - What is needed: Once real analyzers cover enough features, collect ~200 labeled product images per major phenotype category, run the pipeline, compare output to ground truth, and measure accuracy. Acceptance: <5% misassignment rate across all 20 phenotypes with no systematic error on any single category.
  - Answer: still FROZEN: Premature. Revisit after per-feature Analyzer stubs are substantially resolved and BypassPhenotypes flip is planned. A lighter first-pass check (before/after `prism-evidence-report` diff on the standard dataset, not the full 200-images/phenotype protocol) is defined as part of the follow-up ticket that collapses DetOrderRules.json to 5 product types — that's the near-term next step, not a substitute for this item's full acceptance bar.
