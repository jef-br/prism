# Image Classification Todo

-------
- [ ] Define final ImageNGP taxonomy and feature combinations: list all possible ImageNGPs and the ImageFeature values required to derive each phenotype.
  - Answer: FROZEN: Taxonomy is captured in canonical files (ImageNGP.json, ImageRoles.json, imagePhenotypes.md, ImageFeatures.md). No reconciliation action needed at this time.
  - Superseded (T-4700, 2026-07-27): acted on for real, not just re-answered. 10 empty-body analyzer stubs were deleted, taking 22 stub-only features and 6 phenotypes down with them — those features could never be anything but UNKNOWN, and `PhenotypeRuleSet` never treats UNKNOWN as satisfying a `required` condition, so the affected phenotypes were mathematically unreachable, not merely unvalidated. One more feature (`type-of-shot`) was removed alongside them for the same reason despite never having even a stub producer — 23 features gone in total. Taxonomy is now 38 features / 20 phenotypes, all with a real producer (T-4800 re-declared `shadow-present`, fed by `Analyzer_ShadowPresence` off the subject detector). See `jb/docs/ImageNGP/HowToAddAPhenotype.md` for the reconciliation process going forward, and `Analyzers/jbtodo.md`'s "Removed" section for what was deleted.

-------
- [ ] Phenotype production validation: define the protocol and acceptance criteria required before phenotype assignment can be trusted in production.
  - Issue: The 20 phenotypes (26 before T-4700) in `imagePhenotypes.md` were defined from spec and taxonomy documentation without real-image testing. Production-quality assignment requires validation against a representative labeled image set.
  - Update 2026-07-29: the old blocker ("most features are UNKNOWN") no longer holds. After T-4700 and T-4800 every one of the 38 features has a live producer, and `RecordUnknownFeatures` is a phase-1 placeholder the `Refine` chain overwrites, not a stub backlog. The real gap is different: phenotype assignment produced **zero** results on real images until 2026-07-28 (`Refine` threw on every image; `MatchingService`'s non-fatal catch hid it), so nobody has ever seen what it actually assigns.
  - What is needed: ~200 labeled product images per major phenotype category, run the pipeline, compare output to ground truth, measure accuracy. Acceptance: <5% misassignment rate across all 20 phenotypes with no systematic error on any single category. Commission one labeled set — T-4945 needs the same asset.
  - Answer: still FROZEN, but the sequence is now concrete. Near-term next step is the **light** first pass — evidence-harness run on the standard dataset, report the real distribution — which is ticket **T-4970**, blocked until the T-4900 upscale epic completes. T-4955 must be fixed before that.
