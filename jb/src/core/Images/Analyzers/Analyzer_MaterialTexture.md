# Analyzer_MaterialTexture

**Status:** STUB - **Wave:** 3 - **Will write:** `material-texture-visible`

## Proposed workings
Closeup property: high-frequency gradient energy inside the subject box + high crop-tightness means the weave/grain is resolvable. Calibrate the fine-detail threshold against real detail shots (supports `closeup-image` / `model-detail-closeup` evidence).
