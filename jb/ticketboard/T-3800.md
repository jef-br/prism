### T-3800 · Match bracket todos: validate the fuzzy-categorical and totalImageTokens changes
**Status:** Blocked | **Profile:** P1-feature-worker
**Blocked-by:** CiMini expansion (root `jbtodo.md`, **no ticket owns it**).
**Review:** Approve (2026-07-25) on `e2e1f84`+`f40beed` — **the review gate is already satisfied**; the
missing empirical validation was recorded as a known caveat, not a defect.

All code for both remaining `jbtodo.md` items is on main and unchanged. Two of the four original items
closed 2026-07-25 (decisions in `jb/docs/PRISM-match.md`); the two below are implemented and
unit-tested, with only real-data validation still missing. A 2026-08-05 measurement on JBComplete sized
the gap and turned up two new blockers, both now tracked as their own tickets.

**Problem(s):**
- **`SemanticMatcher` (Bracket 4) is confirmed unexercised on real data** (investigated: 2026-08-05, root
  cause identified). Measured on JBComplete, 2026-08-05 (100 images, in-process harness, classification
  on so Bracket 4 was genuinely reachable — `Invoke-CiPipeline -Mode Match` passes `SkipClassification`,
  which disables Bracket 4 by construction and cannot answer this question):

  | Accepted matcher | images |
  |---|---|
  | `NumericMatcher.Bracket1` | 39 |
  | `StringMatcher.Bracket3` | 26 |
  | *(unmatched — all `MATCHES_MULTIPLE_FAMILYIDS`)* | 25 |
  | `NumericMatcher.SubstringRescue` | 5 |
  | `SiblingPropagator` | 3 |
  | `NumericMatcher.Bracket2` | 2 |
  | **`SemanticMatcher` (Bracket 4)** | **0** |
  | `NumericMatcher.Bracket2-Permuted` / `Bracket2-Intersect` / `FilenameToCellMatcher` | 0 |

  The README's claim holds, now measured rather than asserted. `IMG_7710.jpg` (README §6.1) is a
  *described* case, not a file in the folder, and nothing in JBComplete is a tweak away either: Bracket
  4's remit is 0-image families, and the only two families with no images (`98226972`, and `98636325`
  reachable only via the near-miss `E129` files) are both claimed earlier in the waterfall — `98636325`
  by Bracket 3 ([[T-5100]]) and `98226972` by nothing, because the images that should reach it are the
  leetspeak files that tokenise to nothing and KO as ambiguous first. So the blocker is a missing image,
  not a missing measurement. Two further findings from the same run are now their own tickets, and both
  would have to land before a Bracket-4 image could be trusted to reach Bracket 4 at all: [[T-5100]]
  (Bracket 3 accepts on generic tokens when the discriminating one resolves to nothing) and [[T-5110]]
  (`FilenameToCellMatcher` has 0 accepts because it cannot see a filename inside free text).
- **CiMini lacks the two cases needed to validate items 1 and 4** — open, blocks the two `Test(s)`
  bullets below and is this ticket's `Blocked-by` gate. Needed: a Bracket-3 fuzzy case (filename `grey`,
  Excel `gray`) and an image that actually reaches Bracket 4 — the Bracket-4 image still has to be
  authored, since no existing asset in any dataset reaches it. Caveat: expanding CiMini does not require
  re-blessing the goldens for these code changes — `expected-match.json` holds only
  `SourceReference → FamilyId`, and `expected-manifest.json` adds only `Status`/`FinalFileName`/
  `DetOrder`; neither carries features, phenotypes, or subject geometry, so [[T-4800]] did not
  invalidate them. Update 2026-08-06: CiMini's content was replaced wholesale by the former JBComplete
  dataset (CiGolden and JBComplete both retired). `expected-manifest.json` didn't exist for JBComplete's
  content, so it was recaptured fresh from a live run over the new 100-source set — not a re-bless of
  drift. The 3 rows for family `94613033` (the `Pareo_exotica_*` images, [[T-5080]]'s known CLIP
  orientation bug) were hand-restored to their pre-merge values rather than left at whatever the buggy
  live run produced, so the golden still stays red on that known defect. `expected-match.json` was left
  untouched — it's JBComplete's own hand-authored, partly-aspirational file (22 deliberate rejections,
  some cases stated as "expected to fail on day one"), not a captured-actual snapshot.

**Test(s):**
- **Item 1 — fuzzy categorical matching** (`StringMatcher.CollectFuzzyCategoricalEvidence`): run
  before/after and confirm the fuzzy case matches in Bracket 3, and confirm the guardrails still reject
  distance-2 / sub-4-char / non-categorical hits. Checked 2026-08-05: the README §6.3 "live fuzzy case"
  does not test this. The data is exactly as the README describes — `C153KB460011_Cedric_City_Grey_*.png`
  (3 files) say `Grey`, family `99147533` carries Color `Gray` (distance 1), and the files belong to
  `99147525` by reference — but all three match at Bracket 1 on the numeric token `460011`, so the
  waterfall never reaches Bracket 3 and `CollectFuzzyCategoricalEvidence` is never invoked. This confirms
  only the weaker half of §6.3's question (a fuzzy colour match cannot override a reference), and
  confirms it structurally (bracket order), not behaviourally. The fuzzy accept path and its distance-2 /
  sub-4-char / free-text guardrails remain untested on real data — a filename with a fuzzy colour and no
  usable reference number is still needed (see the CiMini-expansion `Problem(s)` bullet above). Not yet
  run.
- **Item 4 — `totalImageTokens` precision** (`SemanticMatcher` → `stringMatcher.CountFilenameTokens`):
  confirm no accept/reject flips near `SemanticThreshold`. Blocked today because 0 of CiMini's 100
  goldens reach Bracket 4 (measured as JBComplete, 2026-08-05, before the 2026-08-06 CiMini merge — see
  the Bracket-4 `Problem(s)` bullet above for the full measurement and root cause). Not yet run.
- **Run the pipeline-integration project in isolation when validating**, not via
  `dotnet test jb/src/PRISM.sln`. The full-solution run fails all 7 `PipelineIntegrationTests.CiMini_*`
  for an unrelated reason ([[T-4942]]: GPU contention between parallel test projects); the project passes
  142/142 on its own. This is a pre-existing, separately tracked issue, not something these two
  validations need to fix.

**Closing:** once the `Test(s)` above are run, close both `jbtodo.md` items per the todo lifecycle, then
run `/ticket-finish`.

**Files:** `jb/src/core/Services/Matching/Match/jbtodo.md`, `jb/src/core/Services/Matching/Match/StringMatcher.cs`, `jb/src/core/Services/Matching/Match/SemanticMatcher.cs`, `jb/docs/PRISM-match.md`, `test/datasets/CiMini/`.
