### T-3800 · Match bracket todos: validate the fuzzy-categorical and totalImageTokens changes
**Status:** Blocked | **Profile:** P1-feature-worker
**Blocked-by:** CiMini expansion (root `jbtodo.md`, **no ticket owns it**).
**Review:** Approve (2026-07-25) on `e2e1f84`+`f40beed` — **the review gate is already satisfied**; the
missing empirical validation was recorded as a known caveat, not a defect.

All code is on main and unchanged. Two of the four original `jbtodo.md` items closed 2026-07-25
(decisions in `PRISM-match.md`). The two below are implemented and unit-tested; only real-data
validation is missing.

**Measured on JBComplete, 2026-08-05** (100 images, in-process harness, **classification on** so
Bracket 4 was genuinely reachable — `Invoke-CiPipeline -Mode Match` passes `SkipClassification`, which
disables Bracket 4 by construction and cannot answer this question):

| Accepted matcher | images |
|---|---|
| `NumericMatcher.Bracket1` | 39 |
| `StringMatcher.Bracket3` | 26 |
| *(unmatched — all `MATCHES_MULTIPLE_FAMILYIDS`)* | 25 |
| `NumericMatcher.SubstringRescue` | 5 |
| `SiblingPropagator` | 3 |
| `NumericMatcher.Bracket2` | 2 |
| **`SemanticMatcher` (Bracket 4)** | **0** |
| `NumericMatcher.Bracket2-Permuted` / `Bracket2-Intersect` / `FilenameToCellMatcher` | 0 |

**Bracket 4 is confirmed unexercised — the README's claim holds, now measured rather than asserted.**
`IMG_7710.jpg` (README §6.1) is a *described* case, not a file in the folder. Nothing in JBComplete is
a tweak away either: Bracket 4's remit is 0-image families, and the only two families with no images
(`98226972`, and `98636325` reachable only via the near-miss `E129` files) are both claimed earlier in
the waterfall — `98636325` by Bracket 3 ([[T-5100]]) and `98226972` by nothing, because the images that
should reach it are the leetspeak files that tokenise to nothing and KO as ambiguous first. **So item 4
stays blocked, and the blocker is a missing image, not a missing measurement.**

Two further findings from the same run, both now ticketed, both of which would have to land before a
Bracket-4 image could be trusted to reach Bracket 4 at all: [[T-5100]] (Bracket 3 accepts on generic
tokens when the discriminating one resolves to nothing) and [[T-5110]] (`FilenameToCellMatcher` has 0
accepts because it cannot see a filename inside free text).

**What is left:**
- [ ] **Expand CiMini** with the two missing cases: a Bracket-3 fuzzy case (filename `grey`, Excel
      `gray`) and an image that actually reaches Bracket 4. The Bracket-4 image still has to be
      authored — no existing asset in any dataset reaches it.
- [ ] **Item 1 — fuzzy categorical matching** (`StringMatcher.CollectFuzzyCategoricalEvidence`). Run
      before/after; confirm the fuzzy case matches in Bracket 3 and the guardrails still reject
      distance-2 / sub-4-char / non-categorical hits.
      **The §6.3 "live fuzzy case" does not test this — checked 2026-08-05.** The data is exactly as
      the README describes: `C153KB460011_Cedric_City_Grey_*.png` (3 files) say `Grey`, family
      `99147533` carries Color `Gray` (distance 1), and the files belong to `99147525` by reference.
      But all three match at **Bracket 1** on the numeric token `460011`, so the waterfall never
      reaches Bracket 3 and `CollectFuzzyCategoricalEvidence` is never invoked. What this *does*
      confirm is the weaker half of §6.3's question — a fuzzy colour match cannot override a
      reference — and it confirms it structurally (bracket order), not behaviourally. The fuzzy
      accept path and its distance-2 / sub-4-char / free-text guardrails remain untested on real
      data, so a filename with a fuzzy colour and **no** usable reference number is still needed.
- [ ] **Item 4 — `totalImageTokens` precision** (`SemanticMatcher` → `stringMatcher.CountFilenameTokens`).
      Confirm no accept/reject flips near `SemanticThreshold`. Blocked today because 0 of CiMini's 100
      goldens reach Bracket 4 (measured as JBComplete, 2026-08-05, before the 2026-08-06 merge — see
      below).
- [ ] Close both `jbtodo.md` items per the todo lifecycle, then `/ticket-finish`.

**Two things to know first:**
1. **The goldens do not need re-blessing for code changes.** `expected-match.json` holds only
   `SourceReference → FamilyId`; `expected-manifest.json` adds only `Status`/`FinalFileName`/`DetOrder`.
   Neither carries features, phenotypes or subject geometry, so [[T-4800]] did not invalidate them.
   **Update 2026-08-06:** CiMini's content was replaced wholesale by the former JBComplete dataset
   (CiGolden and JBComplete both retired). `expected-manifest.json` didn't exist for JBComplete's
   content, so it was recaptured fresh from a live run over the new 100-source set — not a re-bless of
   drift. The 3 rows for family `94613033` (the `Pareo_exotica_*` images, [[T-5080]]'s known CLIP
   orientation bug) were hand-restored to their pre-merge values rather than left at whatever the buggy
   live run produced, so the golden still stays red on that known defect. `expected-match.json` was
   left untouched — it's JBComplete's own hand-authored, partly-aspirational file (22 deliberate
   rejections, some cases stated as "expected to fail on day one"), not a captured-actual snapshot.
2. **Run the pipeline-integration project on its own.** `dotnet test jb/src/PRISM.sln` fails all 7
   `PipelineIntegrationTests.CiMini_*` for an unrelated reason ([[T-4942]]: GPU contention between
   parallel test projects). The project passes 142/142 in isolation.

**Files:** `jb/src/core/Services/Matching/Match/jbtodo.md`, `jb/src/core/Services/Matching/Match/StringMatcher.cs`, `jb/src/core/Services/Matching/Match/SemanticMatcher.cs`, `jb/docs/PRISM-match.md`, `test/datasets/CiMini/`.
