### T-2600 · M5 Classify groundwork
**Status:** Blocked | **Profile:** P0-orchestrator
**Blocked-by:** [[T-4970]] (phenotype validation, in Review).
Tracks the 2 items in `jb/src/core/Services/Matching/Classify/jbtodo.md`.

**Item 1 — taxonomy reconciliation: settled.** [[T-4700]] trimmed to real/reachable features; [[T-4800]]
re-declared `shadow-present`; [[T-4970]] dropped `occlusion-level` and added `back-on-model-partial`.
Stands at **21 phenotypes**, every feature with a live producer. Process documented in
`jb/docs/ImageNGP/HowToAddAPhenotype.md`. Tick this box when item 2 closes.

**Item 2 — phenotype production validation: the only open item.** Measured twice on 2026-07-30 — full
write-up in `jb/docs/ImageNGP/phenotype-assignment-validation.md`. Rule fixes moved SPACINI29 from
correct 15 / wrong 38 to correct 33 / wrong 13 with no threshold change; the 13 survivors are all
upstream ([[T-4990]] detector under-count, [[T-5000]] filename orientation, CLIP orientation argmax 74%).

**`BypassPhenotypes` is gone** (removed 2026-07-31, commit `416ae33`, [[T-5010]]) and CLIP thresholds
were lowered the same day (commit `97326fe`: `hero-orientation` 0.60→0.33, `head-visible` 0.65→0.25,
`body-visible` 0.60→0.10, `OrientationConfidence` 0.75→0.60). **Neither change has been re-measured.**
Three defects that were dormant *because* of the bypass are now live:
1. [[T-4955]] — derived edge features go stale on promotion (42% of SPACINI29 self-contradictory).
2. `FinalOutputSize.RoutesToCenterAndStretch` omits the `SelectedPhenotype is null` half of
   `SelectTransformer` Step 1 (from the archived T-4910 review).
3. `Tx_ProblemImageProcessor` derives output metadata from the unscaled original-resolution field
   (from the archived T-4920 review) — and Step 1 now sends every null-phenotype image there.

**Ordering rule — permanent.** `SubjectDetector` runs in `ImageFeatureAnalyzer.Refine` wave 3, *before*
`FinalizePhenotype` (`ImageFeatureAnalyzer.cs:152-157`). Any detector-backed feature written after the
rules evaluate reads UNKNOWN forever, making every phenotype requiring it unreachable.

**The classical-CV subject producer stays for now** — revisited only once the pipeline is up and running
(likely alongside ComfyUI image generation). Relates to archived T-4810, current [[T-4000]], and a
listing in `Analyzers/jbtodo.md`.

**What is left, in order:**
1. **Re-measure at the new thresholds and with the bypass removed.** Both landed unvalidated; the
   0.30-bar figures in the doc predict but do not confirm them. This is the immediate next step and it
   also closes [[T-5010]]'s verification and unblocks a [[T-4970]] review verdict.
2. Fix [[T-4955]] — tuning against a snapshot that contradicts itself on 42% of images is tuning noise.
3. Fix the two now-live routing defects listed above.
4. Then the full bar: labeled set, confusion matrix, <5% misassignment across 21 phenotypes, no
   systematic error on any one. Commission **one** labeled set — [[T-4945]] needs the same asset.
   Candidates staged in `test/datasets/CiGolden/candidates/` (22 images from FILA94, MMERO26, HEROAUT3,
   SPACINI29, described in that folder's README); six cases still have no source in the repo.

**Ground truth exists for SPACINI29** — `test/datasets/SPACINI29/RAW IMAGES/dataset notes.md`,
user-authored, do-not-edit. It already corrected two claims taken on faith: no image in the dataset is
fully in frame, and `intersection-count` is only 76% accurate.

Per-feature CLIP confidence calibration stays parked here (`AGENTFEEDBACK.md` S109): new confidence
literals get named-const treatment, not config, until this ticket resolves.

M5 gate: item 1 ✅; ONNX shared session ✅ (2026-06-29); item 2 closes after step 1 above.

**Files:** `jb/src/core/Services/Matching/Classify/jbtodo.md`, `jb/src/core/Services/Matching/Classify/ImageFeatureAnalyzer.cs`
