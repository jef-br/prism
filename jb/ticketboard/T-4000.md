### T-4000 · Per-feature Analyzer calibration backlog (index ticket)
**Status:** Ready | **Profile:** P0-orchestrator
**Tracks:** `jb/src/core/Services/Matching/Analyzers/jbtodo.md`

**One live backlog item: 11 implemented analyzers, each with an open calibration/validation question in
its own `.md`** — `Analyzer_ProductType`, `Analyzer_FilenameEvidence`, `Analyzer_HasHuman`,
`Analyzer_SubjectGeometry`, `Analyzer_DominantColors`, `Analyzer_ProductColor`, `Analyzer_BackgroundColor`,
`Analyzer_Exposure`, `Analyzer_MultipleProducts`, `Analyzer_Interior`, `Analyzer_IsIllustration`.
[[T-5000]] is the first of these split out into its own ticket; [[T-5050]]
(`Analyzer_MultipleProducts`) is the second, opened 2026-08-05.

**Split order, decided 2026-08-05 by reading all 11 open questions against what data exists in-repo.**
The constraint is not importance, it is whether the question can be *answered* today — seven of the
eleven need labelled colour/exposure ground truth that only the purpose-built dataset will provide.

*Answerable now (split these first):*
1. ~~`Analyzer_FilenameEvidence`~~ → [[T-5000]], done.
2. ~~`Analyzer_MultipleProducts`~~ → [[T-5050]]. Measured symptom, in-repo footwear + bag data.
3. `Analyzer_HasHuman` — the partial-body recall question has ground truth already: SPACINI29's notes
   name the **12 images showing only the bottom half of a human**, which is exactly the legs-only
   apparel case, and [[T-4970]] found the one miss (`23211095_35_A.jpg`, a draped poncho where YOLO
   returned zero people). `HeroPersonMinArea = 0.15` can be swept against those 86 images the same way
   [[T-4990]] swept the edge detector.
4. `Analyzer_Interior` — a code/doc contradiction rather than a calibration sweep, and JBComplete
   closed the `interior-shot` case five ways (`OMB-E129-TGV_4`, `OMB-E166-BV_4`, `OMB-E180-BV_5`,
   `OMB-E181-CVW_5/6`).
5. `Analyzer_ProductType` term-collision audit — VINGINO79 (2,847 images) and JBComplete now supply
   the real batch vocabulary the audit was waiting for. The vocabulary-unification half stays blocked
   by the project-reference direction and is a separate call.

*Blocked on the purpose-built dataset (do not split yet):* `Analyzer_DominantColors`,
`Analyzer_ProductColor`, `Analyzer_BackgroundColor`, `Analyzer_Exposure` — all four ask "is this
palette/threshold right", and nothing in the repo carries labelled colour or exposure truth.

*Not ticket-worthy:* `Analyzer_IsIllustration`'s only open item was a stale doc path. Fixed directly
2026-08-05 along with the same class of error in `PRISM-classify.md`; its checkbox is now closed.

**Cross-cutting (4):** retire `ImageOrderer.ResolveProductType`'s value-sniffing fallback once
`Analyzer_ProductType` is validated; unify `ProductTypeMap.json` / `TranslationDictionary.json`
vocabulary; segmentation-model milestone for true coverage-ratio masks; standardize CLIP-vs-analyzer
write precedence.

**Closed by earlier work, do not re-open:** the 10 stub analyzers were **deleted** by [[T-4700]]
(2026-07-27), not left unimplemented — their UNKNOWN features made 6 phenotypes mathematically
unreachable. Their proposed workings are preserved in `Analyzers/jbtodo.md`'s "Removed" section and in
git history; re-introduce one at a time, gated on a reliable DetOrderRules catch-all. The
"centralize per-analyzer `*Config.cs`" item was resolved the other way by T-4400's S109 pass
(single-consumer configs folded as nested `Config` types into their owning `Analyzer_*.cs`;
`ColorAnalyzerConfig`/`YoloAnalyzerConfig` stay standalone as genuinely multi-consumer).

**This is an index, not a unit of work.** Orchestrator splits item 1 into per-analyzer tickets as
calibration work is prioritized.

**Acceptance:** each of the 11 analyzers' open question is answered and its `jbtodo.md` checkbox ticked;
`jbtodo.md` reflects real remaining state continuously, not batch-updated at the end.

**Files:** `jb/src/core/Services/Matching/Analyzers/jbtodo.md`, `.../Analyzers/*.md`, `.../Analyzers/*.cs`.
