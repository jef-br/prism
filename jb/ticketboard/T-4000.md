### T-4000 · Per-feature Analyzer calibration backlog (index ticket)
**Status:** Ready | **Profile:** P0-orchestrator
**Tracks:** `jb/src/core/Services/Matching/Analyzers/jbtodo.md`

Index ticket for the Analyzers module's calibration backlog: one open calibration/validation question
per implemented analyzer (11 total, one `.md` each, tracked in
`jb/src/core/Services/Matching/Analyzers/jbtodo.md`). It is not itself a unit of work — the orchestrator
splits individual questions into their own per-analyzer tickets as calibration work is prioritized;
[[T-5000]] and [[T-5050]] are the first two splits (see Problem(s)).

**Problem(s):**
- Analyzer_ProductType (answerable now, open) — the term-collision audit was blocked on real batch
  vocabulary; VINGINO79 (2,847 images) and JBComplete now supply it, so the audit itself is unblocked as
  of the 2026-08-05 review. This merges with the cross-cutting "unify `ProductTypeMap.json` /
  `TranslationDictionary.json` vocabulary" item: that vocabulary-unification half stays blocked pending a
  decision on project-reference direction, separate from the now-answerable audit.
- Analyzer_FilenameEvidence (answerable now) — resolved: split off into [[T-5000]], done.
- Analyzer_MultipleProducts (answerable now) — resolved: split off into [[T-5050]] (opened 2026-08-05);
  the underlying question was a measured symptom, answerable from in-repo footwear + bag data.
- Analyzer_HasHuman (answerable now, open) — the partial-body recall question: ground truth already
  exists. SPACINI29's notes name the 12 images showing only the bottom half of a human (the legs-only
  apparel case), and [[T-4970]] found the one miss, `23211095_35_A.jpg` (a draped poncho where YOLO
  returned zero people). Open question: whether `HeroPersonMinArea = 0.15` is the right threshold given
  this evidence — see Test(s) for the planned sweep.
- Analyzer_Interior (answerable now, open) — a code/doc contradiction rather than a calibration sweep;
  JBComplete closed the interior-shot case five ways: `OMB-E129-TGV_4`, `OMB-E166-BV_4`, `OMB-E180-BV_5`,
  `OMB-E181-CVW_5/6`.
- Analyzer_DominantColors, Analyzer_ProductColor, Analyzer_BackgroundColor, Analyzer_Exposure (blocked on
  the purpose-built dataset, do not split yet) — all four ask "is this palette/threshold right"; nothing
  in the repo currently carries labelled colour or exposure ground truth to answer them.
- Analyzer_IsIllustration (not ticket-worthy) — resolved 2026-08-05: the only open item was a stale doc
  path; fixed directly, along with the same class of error in `jb/docs/PRISM-classify.md`; its
  `jbtodo.md` checkbox is now closed.
- Analyzer_SubjectGeometry — listed among the 11 analyzers with an open calibration/validation question,
  but the 2026-08-05 split-order review below did not address it; whether it is answerable-now or
  blocked-on-dataset is undetermined as written in the source ticket.
- Cross-cutting, open (3 of the original 4 items — the 4th, vocabulary unification, is folded into the
  Analyzer_ProductType bullet above): retire `ImageOrderer.ResolveProductType`'s value-sniffing fallback
  once Analyzer_ProductType is validated; add a segmentation-model milestone for true coverage-ratio
  masks; standardize CLIP-vs-analyzer write precedence.

**Decision(s):**
- Split order for the 11 analyzers (decided: 2026-08-05, by reading all 11 open questions against what
  data exists in-repo) — prioritize by answerability, not importance: 7 of the 11 open questions need
  labelled colour/exposure ground truth that only the purpose-built dataset will provide, so those wait
  (see the grouped Problem(s) bullet above); the rest split first, in the order listed in Problem(s).
- Centralize-per-analyzer-`*Config.cs` item (decided: resolved the other way, via T-4400's S109 pass) —
  single-consumer configs folded as nested `Config` types into their owning `Analyzer_*.cs`;
  `ColorAnalyzerConfig`/`YoloAnalyzerConfig` stay standalone as genuinely multi-consumer.

**Test(s):**
- Sweep `HeroPersonMinArea = 0.15` against SPACINI29's 86 images, the same method [[T-4990]] used to
  sweep the edge detector — validates the Analyzer_HasHuman threshold (not yet run).
- Acceptance check for this index ticket: each of the 11 analyzers' open question is answered and its
  `jbtodo.md` checkbox ticked; `jbtodo.md` must reflect real remaining state continuously, not be
  batch-updated at the end (ongoing, not yet complete).

Closed by earlier work, do not re-open: the 10 stub analyzers were **deleted** by [[T-4700]]
(2026-07-27), not left unimplemented — their UNKNOWN features made 6 phenotypes mathematically
unreachable. Their proposed workings are preserved in `Analyzers/jbtodo.md`'s "Removed" section and in
git history; re-introduce one at a time, gated on a reliable DetOrderRules catch-all.

**Files:** `jb/src/core/Services/Matching/Analyzers/jbtodo.md`, `.../Analyzers/*.md`,
`.../Analyzers/*.cs`, `jb/docs/PRISM-classify.md`.
