## daily-brief.md
- Create or overwrite `daily-brief.md` at the repo root on every session.
- Heading is always `# Daily Brief`, no date, no timestamp.
- Commit and push directly to `main`, no branch: `git add daily-brief.md && git commit -m "docs: update daily-brief.md" && git push origin main`
- 
