# Daily Brief

##### Changed
- **T-4900 epic + all five children are now `Done` with reviewer Approve.** T-4910 and T-4920 — the two still at `Review` in the last brief — cleared the P4 gate on 2026-07-30 (T-4905/T-4930/T-4940 already on 07-29). The T-4910 reviewer re-implemented the forward/inverse canvas pair in a throwaway console app and brute-forced the true minimum across margins 0.0001–0.1999 × targets 1–2000: zero mismatches, worst-case 3 iterations. T-4920's three numeric claims (geometry-follows-pixels stays non-negative because `box.Width ≤ origW` and rounding is monotonic; the 740/739 boundary; the 601/602 bleed-KO window) were each re-derived by hand and hold exactly. One config finding fixed in-review: `MaxLanczosOnlyUpScaleFactor` was `{ get; private set; }`; `private set` cannot carry `required` (CS9032), so it is now `required … { get; init; }`.
- **T-4970 is measured — and the first pass's own remedy was tested and proven wrong.** The first pass said "7% coverage, fix is two config thresholds." Lowering them was actually tried: coverage rises 7% → 62% and the extra assignments are mostly wrong (at shipped config 5 phenotypes assigned to scorable images, all 5 wrong). The real blockers were in the *rules*, not the thresholds, and all three are now fixed with **no threshold touched**: `occlusion-level` deleted as an alias of `intersection-count` (its producer derived it from nothing else, so the 13 rules now state the count directly — behaviour-identical); `model-detail-closeup` narrowed to `intersection-count >= 3` (it was firing on any edge-cropped model shot — 33/86 claimed, none an honest detail crop); `back-on-model-partial` added (**21 phenotypes now, not 20**). Measured on SPACINI29 against the user's hand-verified labels: **correct 15 → 33, wrong 38 → 13 from rule changes alone**. Verdict: **do NOT flip `BypassPhenotypes`**. T-4970 now carries a real code diff (ImageRoles/ImageNGP/DetOrderRules/ClassifyConfig/ImageFeatureAnalyzer + 4 test files) and sits at `Review` awaiting a verdict.
- **First hand-verified ground truth in the repo:** `test/datasets/SPACINI29/RAW IMAGES/dataset notes.md` (user-authored, do-not-edit). It corrected claims taken on faith — no SPACINI29 image is fully in frame (so the earlier "3 front-full + 3 back-full" labels were wrong; every image is a partial), `intersection-count` is 65/86 = 76% accurate with every error an under-count, `hero-is-human` 85/86.
- **Four new tickets fell out of the T-4970 second pass, all Ready.** T-4980: the CiMini `-Mode Full` golden is red — T-4920's too-small KO now measures the *promoted* box, and CARDIGAN_MAGENTA76_DETAIL's promoted box is 471px, under the 570px floor, so an image the golden expects to ship KO's on `PREPROCESS_TOO_SMALL`; worse, `dotnet test` cannot see it (nothing in any test project reads the committed goldens — only the pwsh script does). T-4990: `SubjectEdgeDetector` under-counts frame intersections on 21/86 images, every error an under-count (points at a threshold or edge-strip width, not noise), and `intersection-count=0` hard-gates 12 of the 21 phenotypes. T-5000: `Analyzer_FilenameEvidence` writes orientation at a fixed 0.75 from bare `top`/`bottom`/`back` tokens — 15 of 16 matches across all 14,427 test images are false positives (garment nouns, not camera views), and 0.75 outscores every CLIP orientation reading (max 0.582) so it wins outright. T-5010: `Tx_CenterAndStretch` is Step 3 of `SelectTransformer`, reachable only when the subject touches no edge — but 86/86 SPACINI29 images touch at least one edge, so the treatment the user says the bulk of a catalogue needs is unreachable for all of them.
- **T-4955 reprioritized from "cleanup before the flip" to "prerequisite before any rule/threshold tuning."** T-4970 measured it: a with-Transform run leaves 36/86 (42%) of images with `intersects-*` flags that contradict `intersection-count` (0/86 without Transform). Because `front-on-model-partial` gates on the flags while `front-on-model-full-product` gates on the count, one image can satisfy both mutually-exclusive rules and first-rule-wins picks wrong. Tuning against a snapshot that contradicts itself on 42% of images is tuning against noise.
- **New root-level jbtodo:** "Phenotype validation needs a purpose-built dataset." No dataset here exercises more than a sliver of the 21-phenotype taxonomy (SPACINI29 has a positive case for only 3), so the M11 <5% bar and the `BypassPhenotypes` decision are gated on a measurement nobody can currently take. Spec is one image per bullet in `test/datasets/CiGolden/`; 22 candidates already staged, 6 cases still have no source; the same asset serves T-4945 and T-4948.
- New doc `jb/docs/ImageNGP/phenotype-assignment-validation.md` (indexed in PRISM-index).

##### Next steps
- Run the P1 reviewer gate on T-4970 — it now touches code and cannot reach `Done` without a verdict. Point the reviewer at the `occlusion-level` → `intersection-count` substitution (claimed behaviour-identical across all 13 rules) and the bottomwear det5 insertion that shifts lifestyle/label/material one slot later for existing customers' filenames.
- Decide T-4980 before recapturing anything: is a 471px promoted box on a legitimate detail shot a correct KO (recapture the golden) or a T-4920 defect (the 570px floor was written for whole-product shots)? The same floor governs T-4948 — decide once.
- Close T-4980's visibility half regardless — an xUnit test that diffs the committed goldens, or a CI step that runs `Invoke-CiPipeline.ps1`; today a golden regression is invisible to `dotnet test`.
- Fix T-4955 next — it is now upstream of threshold selection, not downstream of the flip.
- Hold threshold selection (hero-orientation/head-visible/body-visible) until T-4955 + T-4990 + T-5000 land: all 13 surviving T-4970 errors are those upstream faults, not threshold-shaped, so the doc's curve (0.40 → 100% precision at 24% coverage) can't be trusted until they close.
- Commission the CiGolden purpose-built dataset (root jbtodo) — the single labelled asset T-4970's full bar, T-4945 and T-4948 all wait on; 6 of its cases still need source images.
- T-5010 needs a routing decision before any code (relax Step 2 to 3+ edges vs. top/bottom-only non-disqualifying vs. a bleed-aware variant) — and no new/restructured `Tx_*` without explicit user approval.
- Still open, unchanged: T-4942 (serialize the two GPU test projects `-m:1`, assert a minimum test count so a short crashed run stops reading as green); T-4960 (pure code, unblocked — prefer the T-4830 alpha box over colour-distance in `Analyzer_SubjectGeometry`).

##### Todo updates
- **Classify item 2 — phenotype production validation** ([Classify/jbtodo.md](jb/src/core/Services/Matching/Classify/jbtodo.md)): the Answer is now stale in three ways, all resolvable from landed data. (1) "blocked until the T-4900 upscale epic completes" — the epic is `Done` and T-4970 has run its measurement, so the block is cleared, not pending. (2) The sequence it states ("light first pass = T-4970 → then the full bar after the flip is decided") skipped the reversal that actually happened: T-4970's config-only threshold remedy was tested and is wrong; the fix that moved the numbers was three rule changes, so the full-bar ordering is now rules-fixed (done) → T-4955 → thresholds → CiGolden dataset → confusion matrix, with T-4955 promoted *ahead* of the threshold step. (3) The count "20 phenotypes" is stale — `back-on-model-partial` was added, it is 21. Improved from the landed T-4970 diff, not a guess.
- **Export Todo 6 — `upscale-manifest.json`** ([lib/Export/jbtodo.md](jb/src/core/lib/Export/jbtodo.md)): the four-outcome status list's `KoTooSmall` = "salient object below `MinInputSizeInPixels`" is now under-specified by landed T-4920 code. The KO measures the **promoted** subject box, not the legacy salient box, so which images land in `KoTooSmall` changed the moment the epic landed — CARDIGAN_MAGENTA76_DETAIL, a 959×1013 shot with a 471px promoted box, is the first casualty and it broke the CiMini golden (T-4980). The manifest's `KoTooSmall` reason string, and any "% upscaled" denominator, must key off the promoted box's dimension and be reproducible against whichever box promotion produced — not the original salient box a reader would assume. Improved from landed code, not a guess.
- **Analyzers — `Analyzer_FilenameEvidence` "token list to config?"** ([Analyzers/jbtodo.md](jb/src/core/Services/Matching/Analyzers/jbtodo.md)): the open question is no longer abstract. T-5000 measured it — there are already **two** orientation token lists that disagree: `OrientationTokens` hard-coded in the analyzer (25 entries) and `DetOrderKeywordStems.json` (12 groups, carrying `sole`/`outsole`/`close`/`zoom` the analyzer lacks); and the bare `top`/`bottom`/`back` tokens the analyzer matches are 15/16 false positives across all 14,427 test images (garment nouns, not views), written at a fixed 0.75 that outscores every CLIP reading. So "to config?" is really "merge the two lists *and* stop the bare garment-noun tokens writing a confident orientation," now with a measured failure rate behind it. Improved from the T-5000 measurement, not a guess.
- The rest stay genuinely blocked, not under-answered: Match items 1/4 still wait on CiMini Bracket-3/Bracket-4 fixtures (root `jbtodo.md`) — and CiMini's own Full golden is separately red (T-4980), which does not unblock them; Export Todo 4's 7 `Tx_*.cs` still don't write their own param values; Transform HeadCutter Algorithm A still needs measured anatomical crown-offset constants; Generate stays FROZEN behind the ComfyUI backend.
