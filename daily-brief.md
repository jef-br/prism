# Daily Brief

##### Changed
- **T-4800 subject-isolation epic — fully implemented *and* closed in one session (9477524, 2667b29, closed 3d793c8).** The whole Wave 0–3 landed: a persisted `SubjectDetection` contract (box + mask + per-edge intersect + candidate-shadow evidence) on `ImageRecord_LAMBDA`, a classical-CV `SubjectDetector` ported from `process_images.py` (chroma-plane + texture, **lightness deliberately not a criterion** so cast shadow is excluded, hard-shadow strip-by-shape, Canny corroboration), an ingress-alpha path that builds the box from the real transparency channel *before* white-compositing destroys it, `TransformSeed` threading Excel+CLIP colour/background signals into Transform, `PreferSubjectGeometry` promoting a confident detection into the legacy bbox, three seed toggles, and detection/toggle evidence written to `OutputRecord.SafeSummaryText`. The detector runs in `ImageFeatureAnalyzer.Refine` wave 3 (not preprocessing) — the only seam where the Excel seed, the CLIP colour seed, and "phenotype not yet assigned" all hold at once, so a detector-measured `shadow-present` is readable by the rules instead of always reading UNKNOWN. Two production defects that **466 green tests never caught, only the SPACINI29 evidence run did**: (1) the ImageSharp→OpenCvSharp `Mat.SetArray` conversion silently threw on every image and `MatchingService`'s non-fatal `catch { refinementFailed++; }` swallowed it — detection produced nothing and phenotype assignment dropped to 0, surfacing only as a counter nobody reads (fixed with `Marshal.Copy`); (2) `HardShadowEvidenceFraction=0.01` fired on 86/86 images (discriminates nothing, trims 6% off every centred image) — raised to 0.05 (23/86) against the measured distribution. Measured cost: classify stage +18.3s (+11.7%) on SPACINI29 for detection. Tests: Transform 67, Core 127, SubjectDetector green.
- **T-4905 — dynamic-shape ESRGAN, ~12× upscale speedup (69e5ce7, Status: Review).** The committed model pinned input to `[1,3,64,64]`, so an 800px image ran as **625 serialized 64×64 tile Runs** at ~0.2s DirectML dispatch each = 122.9s. The RRDBNet is already spatially size-agnostic internally (pixel_unshuffle derives shape at runtime); only the *declared* input shape forced tiling. A metadata-only edit (weights untouched, bit-identical output) lets it accept whole images in one Run: **122.9s → 10.19s** on the GPU. `Upscaler.RunTiled` now rounds the whole-image tile to even H/W because pixel_unshuffle(2) rejects odd dims.
- **T-4900 upscale epic opened (spec in 69e5ce7).** Upscale is the pipeline's dominant cost; even post-T-4905 it's ~10s/image of real ESRGAN compute — hours on ~1900 images, intolerable on non-GPU desktops. Goal: make ESRGAN opt-in (**default OFF → plain Lanczos, capped +33%**), both paths targeting the *same* exact final-output-size bar computed pre-transform from bbox+intersection+margin. Children T-4910 (exact final-size calculator) → T-4920 (unified scale + ESRGAN/Lanczos gate + KO) → T-4930 (per-job toggle plumbing) → T-4940 (workbench checkbox), blocked-chained.
- **Six follow-up tickets from the T-4800 completion pass (all Ready/Review).** T-4942: `dotnet test PRISM.sln` reds all 7 `PipelineIntegrationTests` in <1ms while each project passes individually — the parallel runner now overlaps the ~95s Matching suite (post stage-move) with Core's pipeline fixture, contending for the same GPU/ONNX sessions; a `Scalar`-into-`CV_8UC3` heap overrun in `SubjectDetectorTests` was root-caused and fixed separately (`Vec3b`), and multi-process production exposure was measured and **cleared**. T-4948 (white-on-white sits under a ~40-grey-level bilateral-filter contrast floor), T-4945 (hard-shadow threshold + centering A/B need labelled data), T-4950 (`SubjectMask` crosses the Matching→Transform HTTP boundary unread), T-4955 (derived edge features go stale after box promotion), T-4960 (wire the alpha box into `Analyzer_SubjectGeometry`'s colour-distance fallback).
- **Pairing skill added (9627dc3)** — `.claude/skills/pair/` + a `pair-guard.ps1` settings hook.

##### Next steps
- Review-approve T-4905 (P4, in Review) — metadata-only lossless diff + even-padding math; unblocks the whole T-4900 chain.
- Then T-4910 (exact final-size calculator, shared pure helper) — the sole Wave-0 unblocker for T-4920; pin it against `Tx_CenterAndStretch`'s worked example (bbox 1800, margin 0.042 → canvas 1948).
- Fix T-4942 before trusting any solution-wide green: serialise the two GPU-touching test projects (`-m:1`) and assert a minimum test count in `ci.yml` — a crashed run still prints `Failed: 0` with a short count.
- T-4960 is pure code and unblocked: prefer the T-4830 alpha box over colour-distance in `Analyzer_SubjectGeometry`, then close that analyzer's standing `.md` todo.
- Board action still open from the epic: thaw T-2600 (user-directed) so a SAM3 / yolo26s-seg producer can sit behind the T-4810 contract.
- Park T-4948/T-4945 until a labelled white-on-white / real-life-background set exists — SPACINI29 is entirely `SOLIDCOLOR`, so toggle (b)'s `HeroDetectionOnSteroids` path has zero real-data coverage.

##### Todo updates
- **Export Todo 4 — `transform-manifest.json`** ([lib/Export/jbtodo.md](jb/src/core/lib/Export/jbtodo.md)): the settled "serialize Tx params into `OutputRecord.SafeSummaryText`" decision now has a **live working precedent** — T-4870 (landed) appends structured detection + toggle evidence into that same field, proving it carries multi-field evidence beyond its old one-line summary. So the todo's open "format of SafeSummaryText" blocker can be anchored to T-4870's actual encoding rather than decided from scratch. Still genuinely open: the 7 `Tx_*.cs` classes don't yet write their own param values (T-4870 carries detection/toggle evidence, not per-Tx params). Improved from landed code, not a guess.
- **Export Todo 6 — `upscale-manifest.json`** ([lib/Export/jbtodo.md](jb/src/core/lib/Export/jbtodo.md)): the four-outcome status capture this todo asks for (`Upscaled`/`Unnecessary`/`KoTooSmall`/`KoExceededMaxScale`) stops being standalone plumbing — T-4920's unified `UpscaleAsync` rewrite must attach an upscale status + exact scale to the record anyway (it reuses `PREPROCESS_UPSCALE_EXCEEDED` and computes the final size via T-4910), so this todo should *consume* that field rather than add its own capture. New caveat the T-4900 design forces: T-4920 introduces a Lanczos-only path and moves the ON path to target the final-image bar, so "percentage upscaled" and the resolution fields must now distinguish two upscale families (ESRGAN vs Lanczos), not one. Improved from the T-4900 board, not a guess.
- **Classify item 2 — phenotype production validation** ([Classify/jbtodo.md](jb/src/core/Services/Matching/Classify/jbtodo.md)): the lighter first-pass check (before/after `prism-evidence-report` diff, not the full 200-images/phenotype protocol) is no longer just unblocked — T-4800's SPACINI29 run **exercised exactly that harness on 86 real images**, and in doing so found and fixed a silent phenotype-zeroing bug (the `Mat.SetArray` failure made `Refine` throw on every image, dropping phenotype assignment to 0 while 466 tests stayed green). That matters specifically here: before the fix, any before/after phenotype diff would have compared zero against zero. So the prerequisite for a meaningful lighter validation now actually holds. Full protocol stays FROZEN. Improved from a landed evidence run, not a guess.
- The rest stay genuinely blocked, not under-answered: Match items 1/4 still wait on CiMini Bracket-3/Bracket-4 fixtures (root `jbtodo.md`); Transform HeadCutter Algorithm A still needs measured anatomical crown-offset constants; Generate stays FROZEN behind the ComfyUI backend.
