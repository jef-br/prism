# Daily Brief

##### Changed
- **The four remaining T-4900 children landed on main (commit `a1fd1c2`), all at `Review` awaiting the P1/P4 reviewer gate.** In the last brief these were opened-and-blocked-chained *specs*; they are now code-complete. T-4910 is the exact final-size calculator (`FinalOutputSize`): the forward `CenterAndStretchCanvasSize` plus an inverse `MinimalScaleToReach` that does **not** solve the canvas formula algebraically — it takes the continuous inverse (provably never above the answer, since floor/even/trim only shrink the canvas) and steps up against the forward function until the bar is cleared, so it converges in ≤3 passes and cannot land a pixel short the way hand-derived algebra can. T-4920 rewrote `UpscaleAsync` to the unified model (minimal scale to clear the *final-image* bar, then the toggle picks resampler + cap only — ESRGAN to `MaxUpScaleFactor`, Lanczos to the new `MaxLanczosOnlyUpScaleFactor`; past the cap → `PREPROCESS_UPSCALE_EXCEEDED`, OFF message appends the ESRGAN remedy sentence). T-4930 added the `AllowEsrganUpscale` per-job flag (omitted = false = Lanczos), read off `matched.Ingest.Parameters` rather than threaded as an argument. T-4940 added the workbench checkbox. Tests green: Core 153, Transform 83, Matching 230, Upscale 17, Generate 10.
- **T-4905 closed — reviewer Approve (2026-07-29).** Was `Review` in the last brief. The reviewer did not take the prose on faith: it loaded both `.onnx` files and hashed all 702 initializers — identical SHA256, identical 1226-node graph, the only difference the declared input shape (`[1,3,64,64]` → dynamic `height`/`width`).
- **Three latent defects the epic uncovered and fixed, each blocking the epic's own premise, not scope creep.** (1) The bounding box was never rescaled after upscale — `UpscaleAsync` enlarged the bytes while `lambda.BoundingBox` stayed in original-image pixels, so `Tx_CenterAndStretch` cropped an original-coordinate rect out of an enlarged image and the canvas never reached 800px anyway; geometry now scales with the pixels. (2) `Tx_CropSquare.Transform` recorded a `CropRectangle` but never touched `ProcessedBytes`, so Export shipped the whole frame while the manifest claimed a square — the route every intersecting image takes under `BypassPhenotypes = true`; it now crops the bytes. (3) Upscale sized against the pre-promotion box; promotion + shadow accounting moved into `ImageTransformer.FinalizeGeometry`, called from `PreprocessAsync` before the upscale decision.
- **Two threshold consequences to know before re-running any evidence.** The centre-and-stretch pass-through threshold is now **740px, not 800** (the final-image bar back-solved through the canvas formula), so 740–800px bboxes that used to be upscaled no longer are — a re-run's KO/upscale counts will not match older evidence. And the Lanczos-only 1.33× cap is **unreachable on that route** at current config (a bbox at the 570px input floor needs only 740/570 = 1.30×), so the OFF-mode KO can fire only on the bleed route, for images whose shorter side is under ~602px. Both fall out of the numbers, not by design, and move if any of `MinInputSizeInPixels`/`MinOutputWidth`/`WhiteSpaceMargin`/either cap changes.
- **T-4970 opened** (light first-pass phenotype validation — evidence-report distribution + verdict, not the full <5% labelled bar), blocked on the T-4900 epic; and **T-2600 rewritten** against main after T-4700/T-4800 (item 1 settled at 38 features / 20 phenotypes, item 2 the sole open item, now routed through T-4970).

##### Next steps
- Run the P1/P4 reviewer gate on T-4910/T-4920/T-4930/T-4940 — all four are code-complete at `Review` and this is the only thing between them and `Done`.
- With the epic code-complete, T-4970 (light phenotype first pass) is one reviewer-approval from unblockable — queue the `prism-evidence-report` run so the `BypassPhenotypes` flip finally has real distribution data.
- Fix T-4942 before trusting any solution-wide green: serialise the two GPU-touching test projects (`-m:1`) and assert a minimum test count in `ci.yml` — a crashed run still prints `Failed: 0` with a short count.
- T-4955 (recompute `intersection-count`/`fully-in-frame`/`occlusion-level` at box promotion, or mark them pre-promotion-only) must land before any `BypassPhenotypes` flip, not before the T-4970 measurement.
- T-4960 is still pure code and unblocked: prefer the T-4830 alpha box over colour-distance in `Analyzer_SubjectGeometry`, then close that analyzer's `.md` todo.
- Park T-4948/T-4945 until a labelled white-on-white / real-life-background set exists — SPACINI29 is entirely `SOLIDCOLOR`, so toggle (b)'s `HeroDetectionOnSteroids` path still has zero real-data coverage.
- Board action still open: thaw T-2600 (user-directed) so a SAM3 / yolo26s-seg producer can sit behind the T-4810 contract.

##### Todo updates
- **Export Todo 6 — `upscale-manifest.json`** ([lib/Export/jbtodo.md](jb/src/core/lib/Export/jbtodo.md)): two of its open questions are now answered by *landed* T-4920/T-4930 code rather than the board. (1) The blocker "confirm no downstream stage mutates `Base.Width/Height` after upscale" is settled — T-4920 deliberately leaves `ImageRecord_Base.Width/Height` unscaled (only `BoundingBox`/`LegacySalientBox` move into the enlarged space), and its own ticket states this is *precisely* to preserve "the original-resolution contract Export's upscale-manifest todo depends on." So this todo's original-resolution field is confirmed safe and intentionally retained, not at risk. (2) The four-outcome status list's `Unnecessary` = "salient dim already ≥ output-width threshold" is now stale: the actual pass-through bar is the **740px final-image projection**, not the 800px `MinOutputWidth`, so `Unnecessary` covers a larger set than "bbox ≥ 800," and status + "% upscaled" must additionally split the ESRGAN vs Lanczos families T-4920 introduced. Improved from landed code, not a guess.
- **Classify item 2 — phenotype production validation** ([Classify/jbtodo.md](jb/src/core/Services/Matching/Classify/jbtodo.md)): the todo's own 2026-07-29 note routes its light first pass through T-4970, "blocked until the T-4900 upscale epic completes." That blocker is now code-complete — all five children implemented (T-4905 `Done`, T-4910/4920/4930/4940 at `Review`, commit `a1fd1c2`), so the epic is one reviewer-gate away from clearing, not an epic away. Nothing about the validation protocol itself changes; the wait is now measured in review passes, not implementation. Improved from the landed epic, not a guess.
- The rest stay genuinely blocked, not under-answered: Export Todo 4's 7 `Tx_*.cs` still don't write their own param values (T-4870 carries detection/toggle evidence only, not per-Tx params); Match items 1/4 still wait on CiMini Bracket-3/Bracket-4 fixtures (root `jbtodo.md`); Transform HeadCutter Algorithm A still needs measured anatomical crown-offset constants; Generate stays FROZEN behind the ComfyUI backend.
