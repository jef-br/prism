# Daily Brief

##### Changed
- **M11 phenotype accuracy measured for the first time.** JBComplete's `expected-phenotype.json` (99 labelled rows) is the first dataset that exercises more than a sliver of the taxonomy, so the M11 bar could finally be scored against real ground truth. Result at shipped config: **30.3% misassignment** (the gate is <5%), **39.4% coverage**, `front-packshot` recall **0/25**, `closeup-image` precision **0/8**. SPACINI29's old 4.7% was never a pass — it only touched 2 of the 18 phenotypes. Two upstream causes account for nearly all the error and **neither is a dataset problem**: [[T-5070]] (`intersection-count = 0` is required by 7 of 18 phenotypes but only 27/100 images satisfy it) and [[T-5080]] (`hero-orientation` is UNKNOWN on 37% and never once outputs `SIDEON`). M11 is now Blocked on those two, not on a missing fixture.
- **JBComplete moved into `test/datasets/` with committed ground truth** — `expected-match.json` + `expected-phenotype.json` (99 rows) + README §3's per-case accounting. Scoring the set is what surfaced the M11 numbers above and opened five defect tickets: [[T-5070]], [[T-5080]], [[T-5090]] (SubstringRescue invents evidence from shot numbers), [[T-5100]] (Bracket 3 steals a neighbour's family), [[T-5110]] (filename-in-cell never fires), plus [[T-5050]] split out of the analyzers todo.
- **T-5020 / T-5030 / T-5040 closed.** Folder names now reach the matcher (were silently dropped before); every input is normalized to JPG-on-white and the separate alpha/transparency path is gone; the phenotype set is pruned **21 → 18** (removed ones had no rule consuming them).
- **T-5060 fixed** (Review): det-slot compaction now sorts by axis order, so CiMini `90861052` matches its golden again — it was reordering a family on compaction.
- **T-4955 / T-4990 / T-5000 landed** (all Review): stale derived edge features recompute at promotion; the detector's two-threshold recalibration lifts intersection counting 65/86 → 84/86; filename orientation now reads only the trailing stem token, killing the false positives. **T-4980 reworked**, **T-5010's 6 red Transform fixtures are green**, **T-4960 closed Obsolete**.

##### Next steps
- Land [[T-5070]] + [[T-5080]] then re-score JBComplete's 99 rows — that single pair is what stands between the 30.3% miss and the M11 gate; it's T-2600's stated next action.
- Clear the Review backlog with a verdict + the outstanding check each: T-4955, T-4990, T-5000, T-5010, T-5060, and T-4942 (confirm `-m:1` + the 500-test floor on a real CI run).
- Fix the two matcher defects the scoring exposed: T-5090 (evaluate every rescue token, KO on contradiction instead of returning on the first) and T-5100 (refuse a match when a discriminating token resolves to no family).
- Author the fixtures no committed dataset still has: a Bracket-4 picture-only case + a reference-free fuzzy-colour image (T-3800), the `illustration-technical-drawing` positive, and a hard-vs-soft shadow twin (T-4945).
- Human-pass the 16 `"Confidence": "low"` rows in `expected-phenotype.json` (README §4.3) — worth ±1.4 points on the headline number, not load-bearing.

##### Todo updates
- **Classify item 2 — phenotype production validation** ([Classify/jbtodo.md](jb/src/core/Services/Matching/Classify/jbtodo.md)): the Answer still reads "still FROZEN … next step is the light first pass — T-4970, blocked until the T-4900 upscale epic completes." All three of those are now history: T-4900 and T-4970 are closed, and the validation the todo was written to demand **has been run** — 30.3% misassignment / 39.4% coverage / `front-packshot` 0/25 on JBComplete's 99-row labelled set. So the answer is no longer "run the pass"; it's "the pass ran and failed the bar, blocked on T-5070 + T-5080, both upstream feature defects, neither fixable in config." Improved from the board's M11 line + T-2600, not a guess.
- **Match item 1 — StringMatcher fuzzy categorical** ([Match/jbtodo.md](jb/src/core/Services/Matching/Match/jbtodo.md)): the Answer ends "Ready for /todo-finish once T-3800 validation is accepted." That validation still can't be run. JBComplete carries the exact data (`C153KB460011_..._Grey` vs family `99147533` Color `Gray`, edit-distance 1) but all three files resolve at **Bracket 1** on the numeric token `460011`, so Bracket 3 never runs and `CollectFuzzyCategoricalEvidence` is never invoked. The fix is on main but has zero real-data exercise; the image still needed is a fuzzy colour with **no usable reference number**. Improved from root jbtodo, not a guess.
- **Match item 2 — SemanticMatcher.totalImageTokens** ([Match/jbtodo.md](jb/src/core/Services/Matching/Match/jbtodo.md)): the Answer says the before/after near `SemanticThreshold` is "still the open validation before /todo-finish." Also still not runnable — `Bracket2-Intersect` has **0 accepts** and Bracket 4 has **no accept** on JBComplete, so no image reaches the code path the fix touches. The fix ships unvalidated on real data; T-3800's Bracket-4 picture-only case is the fixture that would finally exercise it. Improved from root jbtodo, not a guess.
- The rest stay genuinely blocked: Export Todo 4's 7 `Tx_*.cs` still don't write their own param values; Transform HeadCutter Algorithm A still needs measured crown-offset constants; Generate stays FROZEN behind the ComfyUI backend; the Analyzers calibration items (ProductType, HasHuman, DominantColors, …) still want per-analyzer measurement runs.
