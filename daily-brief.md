# Daily Brief

##### Changed
- **T-4110 closed** (moved to archive). `OnnxSessionFactory.Create` is now the single construction point for every `InferenceSession` in PRISM (CLIP, YOLO, Upscale) — no inline session building; ONNX Runtime (`DirectML 1.24.4`) + ImageSharp + OpenCvSharp are pinned centrally via CPM in `jb/src/Directory.Packages.props`. The Lanczos CPU-fallback upscaler and `ImageUpscaler` router were deleted (one `Upscaler` class remains, Real-ESRGAN on every host with the factory picking the execution provider — no algorithm switch on GPU presence). Missing **or corrupt** model assets now fail startup loud, contract pinned by corrupt-file tests in all three service suites.
- **T-4600 closed** (archive): SSE progress events now carry real per-item `Completed`/`Total` counts; Export completion counts scoped to the Export stage.
- **T-4400 now Active, phase-1 Approved** (2026-07-20). `StyleCop.Analyzers` + `SonarAnalyzer.CSharp` wired into every production `.csproj` via `Directory.Build.props` (test projects excluded by `*Tests*` name match to keep S109 off test literals); curated `.editorconfig` severities; `SonarLint.xml` sets S109 allowed magic-number exceptions to `0/1/-1`. SA1402 (one type per file) + SA1649 fixed to zero and CI-gated with `-warnaserror`. Phases 2-4 remain: S109 triage (98 in core), SA1633 header (113), SA1101 `this.`-prefix (472).
- **R7 fixed** (`9df66d7`): Transform's remote-upscale path converted from sync-over-async (`.GetAwaiter().GetResult()` blocking up to processor-count thread-pool threads on network I/O) to `Parallel.ForEachAsync`; `Preprocess`/`Upscale` are now genuinely async, local GPU/CPU upscale stays synchronous (compute, not I/O). Full solution suite green (399/399). All eight T-3300 review findings (R1–R8) now marked FIXED in the root `jbtodo.md`.
- Repo `.gitignore` added (build output, ONNX models, non-CiMini datasets, workbench artifacts); CPM/StyleCop package-ref conflict from merging T-4110 + T-4400/T-4600 resolved.
- Open ticket set is now **T-2600** (M5 Classify, Blocked), **T-3800** (Match todos, Ready), **T-4000** (Analyzer TOC, Ready), **T-4400** (Roslyn analyzers, Active) — T-4110 and T-4600 archived as Done.

##### Next steps
- **T-4400 phase 2 (S109) is the next gated rule** — 98 magic-number literals in core, each either moved to config per the shadow-defaults rule or explicitly justified as structural. It's the priority rule: shadow-defaults enforcement at compiler grade.
- Confirm the SA1101 `this.`-prefix direction with the user before phase 4 — 472 mechanical prefixes contradict the "short, practical" style line; unchanged blocker.
- **/todo-finish the substring-rescue perf todo standalone** — its measurement is complete and it has no CiMini/Bracket-4 dependency, unlike the other two T-3800 Match items; it can close now with no code change.
- **Build the CiMini Bracket-4 expansion (root `jbtodo.md`)** — it's the single blocker for validating the two remaining T-3800 fixes (`totalImageTokens`, fuzzy categorical); the root todo's purpose-built "z" edge case exists precisely to exercise the `totalImageTokens` fix.
- **T-4000:** start `Analyzer_FacePose` (6 features, unblocks the most on-model phenotypes) plus the milestone-independent `AnalyzerConfig.cs` centralization — the config item can start anytime.

##### Todo updates
- Match `totalImageTokens` fix (`stringSignal` no longer leaks candidate-pool size): its CiMini before/after validation is currently **impossible** — the root `jbtodo.md`'s 2026-07-17 run found 0 of 14 CiMini images ever reach Bracket 4 (`SemanticMatcher`), so the golden cannot exercise this fix at all until the root todo's purpose-built "z" edge case is added. The accept/reject-flip validation is gated on the CiMini expansion, not just a run — derived from the root todo's coverage fact, not a guess: [jbtodo.md](jb/src/core/Services/Matching/Match/jbtodo.md).
- Match substring-rescue perf todo: unlike the other two Match items it carries **no** Bracket-4/CiMini dependency — its Stopwatch measurement is already complete (250 unmatched ≈ 336 ms, 2,500 ≈ 1.1 s at 3k-family scale; n-gram index not warranted), so it is closeable via /todo-finish standalone right now while the fuzzy/`totalImageTokens` items wait on data: [jbtodo.md](jb/src/core/Services/Matching/Match/jbtodo.md).
