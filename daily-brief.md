# Daily Brief

##### Changed
- 37 commits since last brief's baseline (172bde6 → 704e2fa). Dominant theme: a config-loading overhaul, not new pipeline behaviour.
- ConfigLoader wave landed (T-4500 master → T-4510 core, T-4520/T-4530 Transform, T-4540 Analyzers; plus T-4200/T-4300). Transform + Analyzer config classes are now `required`, no in-code defaults, one JSON file per area (`transform_Config.json`, `analyzer_Config.json`) — a missing/misspelled key now fails loud at load. `ModelAssetLocator` gained coverage; T-4400 opened to adopt Roslyn analyzers (S109 magic-number rule first).
- HeadCutter's `0.75` cut constant was externalized by T-4200 to `transform_Config.json` `HeadCutter.FaceHeightCutFactor` (bound via required no-default `HeadCutterConfig`, validated to (0,1)). Value unchanged; behaviour identical; it is now a tunable, not a literal.
- T-3900 closed: Order's `DetermineTieBreaker` rescan could name a competitor that wasn't the deciding rival — now names the one that beat the closest rival.
- T-3400 shipped web-workbench dark mode + layout compaction + real-time import/export stage feedback — directly covers root-`jbtodo` web bullets 2 (darkmode), 3–4 (compact review / scrolling), 6 (import/export feedback). Root `jbtodo` still lists them uncut.
- WPF workbench fully deleted (web is the sole frontend; AGENTFEEDBACK 07/10). T-3200 closed (Services test gaps: IIngestService IO/import + IArtifactStore). Review gate added: P1/P4 tickets need a reviewer `Approve` before Done.

##### Todo updates
- Match/jbtodo StringMatcher edit-distance gap: confirmed the doc↔code mismatch is real — zero fuzzy/Levenshtein logic exists in `Services/Matching/Match/`, while `PRISM-match.md:76` explicitly promises "string matching tolerates edit distance." The existing recommendation cites `maxDistance: 1` as a precedent, but production `maxDistance` is **1.478** (`MatchingConfig.json`) and is a TCD (numeric-magnitude tolerance on concatenated digit tokens per `PRISM-match.md:49`), not a character-edit distance — wrong precedent, needs correcting before anyone acts on it. Also worth flagging: reusing `ComputeLevenshteinDistance` (`ModelBuilder.cs:928`) would add a Matching→Excel project reference the 07/08 restructure otherwise avoids.
- Match/jbtodo SubstringRescue perf: two config facts narrow the claimed worst case — production `minSubstringRescueLength = 7` and the pass self-disables at ≤ 0 (`NumericMatcher.cs:433`), and only filename digit tokens ≥ 7 chars qualify as rescue tokens (`NumericMatcher.cs:443`), typically 0–2 per image, not "every leftover token." Still unmeasured — the todo's own "probably fine, unverified" stands, just on a tighter bound.
- Match/jbtodo SemanticMatcher.totalImageTokens: re-verified the code still matches the todo's description verbatim after the restructure (`SemanticMatcher.cs:94-95,120-121`) — `scored.Count` (candidate-family count) really does inflate the `stringSignal` denominator independent of the filename. Nothing new resolvable from code alone; still needs the labeled/CiMini before-after the todo already asks for.
- Transform/Engine/jbtodo HeadCutter answer needs a line updated: the "hardcoded `0.75`" description is now stale — T-4200 moved that constant into `transform_Config.json` as `HeadCutter.FaceHeightCutFactor` (required, no default, validated to (0,1)). Value and behaviour are unchanged; only the description of where it lives needs the edit.
- Everything else unimprovable without guessing: Order/T-2830 answer (a) already implemented (`CompactDetOrder`); Classify + Generate items FROZEN; HeadCutter Algorithm A crown-offset still blocked on the anatomical-ratio deepdive; Analyzers TOC items each defer to per-analyzer .md; root jbtodo (web workbench + Import/Match fusion) is new design work needing your direction, not a fill-in.

##### Next steps
- `dotnet build jb/src/PRISM.sln` + `dotnet test` to confirm the ConfigLoader wave's required/no-default config classes load clean on every area — a mistyped key now hard-fails at startup, worth catching before a real batch.
- Reconcile root `jbtodo` web bullets against what T-3400 already shipped (darkmode, compaction, import/export feedback) so only the genuinely-open items (less-beige palette, drowned-zip-link) remain.
- Apply the four Todo-updates findings above to their jbtodo.md files (StringMatcher precedent correction, SubstringRescue tighter bound, HeadCutter config-path note) once you've reviewed them — this brief only reports the findings, per the "no other file" rule.
- Triage the two root-jbtodo asks: remaining web refinement and Import↔Match fusion to kill double image I/O — both want your scope before an agent picks them up.
