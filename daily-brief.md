# Daily Brief

##### Changed
- The Wave-3/Wave-4 gate the last brief flagged as the sole open blocker is cleared. **T-4550** and **T-4560** are closed and archived (380bcc9): `ImageTransformationResult` folded into `ImageRecord_OUTPUT` (d5c2727), and the ConfigLoader migration landed — `PrismConfigLocator` + `ConfigCache` deleted (5e98be0). Verified gone from `jb/src` (only a stray `Services/Transform/DUMMY FOLDER/goal.md` still names them). Master ticket **T-4500** closed in the same commit.
- **T-3400** closed (8149940) after a dark-mode contrast fix (f9df410). The "shipped-but-not-archived" reconciliation the last brief asked for is done — it's no longer `Ready`.
- Transform config classes moved into `Services/Transform/Admin` + UI tuning (c635332).
- Two new root `jbtodo.md` items raised from the T-3500/rename work: (1) the Import→Match in-memory byte handoff has **no size ceiling and no disk spillover**, so concurrent max-size jobs (`MAXIMUM_REQUEST_SIZE` = 25 GiB) can OOM at the Import→Match boundary as a mystery crash; (2) a docs-vocabulary sweep for stale type/field/path names.
- New user-direction notes on **T-2820** (fix the tied-family det non-determinism via CLIP refinement — model / `ClipPrompts.json` / thresholds — not a deterministic tie-break hack) and **T-2830** (add a toggle-able final det-slot collapse pass). Neither reproduced on 3 consecutive CiMini Full runs during T-4560 verification, but 3 runs can't disprove an intermittent tie.

##### Next steps
- **T-2830 is likely already satisfiable without new code.** The 2026-07-14 direction asks for a "toggle-able final collapse pass"; `ImageOrderer.CompactDetOrder` (called from `Exporter.Run`, gated by `DET-ORDER-GAPS-ALLOWED` = `false` in `Prism_Config.json`) already renumbers each family to contiguous `det0..detN`. Confirm the toggle behaves as intended on a fresh run, then answer the direction as "already built" rather than scoping it as new work: [jb/src/core/Services/Matching/Order/jbtodo.md](jb/src/core/Services/Matching/Order/jbtodo.md).
- **T-2820:** run 5+ consecutive `-Mode Full -Dataset CiMini` (the ticket's own Acceptance bar) before spending effort — last pass's 3 identical runs aren't enough. Pursue the CLIP-refinement direction over freezing an arbitrary tie-break key.
- Kick off the new **docs-vocabulary sweep** — now cheap and unblocked: start from the confirmed `Images/*`→`Services/*` path drifts and the deleted `ImageTransformationResult`/`PrismConfigLocator`/`ConfigCache` names, then extend to PRISM-specific vocab and `GLOSSARY.md`.
- **Import→Match handoff:** before deciding, measure (a) the saved decode's share of total job wall time and (b) peak RSS on a large batch. Cheap safe interim is the todo's option 1 — a config ceiling that leaves the carried bytes null above the limit so Match falls back to disk (pre-T-3500 behavior).

##### Todo updates
- **Docs-vocabulary sweep** — seeded its OPEN answer with two confirmed stale-path instances found this pass: the Order todo points the phenotype-gating fix at `jb/src/core/Images/Classify/jbtodo.md` (real path `jb/src/core/Services/Matching/Classify/jbtodo.md`), and the Transform-Engine todo header uses `jb/src/core/Images/Transform/...` (real `Services/Transform/Engine/...`, as its own answer body already corrects). Both are exactly the `Images/*`→`Services/*` drift the sweep targets — why: concrete instances read straight from existing files, no guessing: [jbtodo.md](jbtodo.md).
- **Match StringMatcher (item 1)** — the recommended solution cites "distance ≤ 1, matching NumericMatcher's own `maxDistance: 1` production value" as precedent; that's wrong twice over. `MatchingConfig.json` sets NumericMatcher `maxDistance` = **1.478**, not 1, and it's a numeric token-character-distance (TCD) float threshold, not an integer Levenshtein bound over categorical strings — a different metric on a different type, so it can't justify a `≤1` edit-distance rule. If typo tolerance is added, bound it on its own terms — why: pins the actual config value and the metric mismatch the prior flag only gestured at: [jb/src/core/Services/Matching/Match/jbtodo.md](jb/src/core/Services/Matching/Match/jbtodo.md).
