# Daily Brief

##### Changed
- **T-4970 is reviewer-Approved (2026-07-31).** The verdict the last brief asked for landed: both attack points were checked against code, not taken on faith. The `occlusion-level` → `intersection-count` substitution is behaviour-identical in 12 of 13 rules (the 13th was never claimed to be); one non-blocking warning only. T-4970 is now eligible for Done — its own "re-measure at the shipped thresholds" is the remaining action, not a review gate.
- **T-4980 got code and a Request-Changes verdict — the fix is dead code.** `d808398` short-circuits the classical-CV `SubjectDetector` to a whole-frame box when `intersection-count == 4` (full-bleed). The reviewer ran the pipeline: `CARDIGAN_MAGENTA76_DETAIL.jpg` now returns `Ok`/`90861052_det2.jpg` and the golden passes — **but not for the reason the code gives.** `ImagePreProcessor.PreprocessAsync` overwrites `lambda.BoundingBox` unconditionally (~line 89) via an independent `DetectSalientBoundingBox` call, so the full-frame box this diff writes is never read. The golden went green by some other path; the committed rationale is wrong. Still `Review`, Request Changes.
- **T-5010 got code and a Request-Changes verdict — restoration correct, suite red.** `416ae33` removes `BypassPhenotypes` (which had forced intersecting images straight to `Tx_CropSquare`, bypassing the real phenotype/det-slot routing that was already implemented but unreachable). Routing is now restored, build clean (14 projects, 0 errors), **but `Prism.Services.Transform.Tests` = 77 passed, 6 failed**, all failing identically: expected `Tx_CenterAndStretch`/`Tx_CropSquare`, got something else. Six stale fixtures must be fixed before re-review. Still `Review`.
- **The three CLIP confidence bars the last brief said to hold were lowered anyway (`97326fe`).** `hero-orientation` 0.6 → **0.33**, `head-visible` 0.65 → **0.25**, `body-visible` 0.6 → **0.1**, and `OrientationConfidence` 0.75 → **0.60**. Rationale in the commit: the prior bars sat above achievable CLIP scores, gating coverage. This is exactly the threshold move the last brief recommended deferring until T-4955/T-4990/T-5000 land — it shipped ahead of them, so the phenotype doc's precision curve (drawn at 0.40+) no longer describes the running config.
- **Ticket board restructured into `jb/ticketboard/`.** The 1132-line root `AGENT-TICKETS.md` is now a ToC (one row per ticket) plus one `T-XXXX.md` file each; `AGENTFEEDBACK.md` moved under `jb/ticketboard/` too. `SubjectDetection` renamed to `SubjectDetectionResult` across contracts/tests. No pipeline behaviour change from either.

##### Next steps
- Close T-4970 (Approve is in) with /ticket-finish, then re-run its coverage measurement against the **new** committed bars (0.33/0.25/0.1) — the old numbers were taken at 0.40+ and don't transfer.
- Rework T-4980 where it actually bites: either move the full-frame box to what `PreprocessAsync` reads, or gate `DetectSalientBoundingBox` on `intersection-count == 4` — today the diff writes a box nothing consumes, and the green golden is coincidental.
- Fix T-5010's 6 red Transform fixtures before asking for re-review — the routing change is correct but no verdict passes over a red suite.
- Close T-4980's visibility half regardless — `dotnet test` still cannot see the committed goldens; only `Invoke-CiPipeline.ps1` reads them.
- Treat all post-tune phenotype numbers as provisional until T-4955 + T-4990 + T-5000 close — the bars moved but the three upstream faults (stale derived features, under-counted intersections, filename false positives) did not.
- Still open, unchanged: T-4942 (serialize the two GPU test projects `-m:1`, assert a minimum test count); T-4960 (prefer the T-4830 alpha box over colour-distance in `Analyzer_SubjectGeometry`); T-4955 remains upstream of any further threshold tuning.

##### Todo updates
- **Classify item 2 — phenotype production validation** ([Classify/jbtodo.md](jb/src/core/Services/Matching/Classify/jbtodo.md)): the Answer's "still FROZEN … T-4970 blocked until the upscale epic completes, T-4955 must be fixed before that" is stale on landed facts. T-4970 has run *and* is reviewer-Approved (2026-07-31), so it is not blocked; and the three confidence bars its measurement targets were themselves committed today (`97326fe`) at 0.33/0.25/0.1, down from 0.6/0.65/0.6. The concrete near-term step is no longer "wait for the epic" — it is re-run the coverage measurement against the new bars, because the doc's earlier precision curve was drawn at 0.40+ and doesn't describe the running config. Improved from the landed review + config, not a guess.
- **Analyzers — `Analyzer_FilenameEvidence` "token list to config?"** ([Analyzers/jbtodo.md](jb/src/core/Services/Matching/Analyzers/jbtodo.md)): commit `97326fe` lowered `OrientationConfidence` 0.75 → 0.60, but that does not resolve the T-5000 false-positive problem the question now carries. Measured CLIP orientation tops out at 0.582, so 0.60 still outscores every real reading — the 15/16 garment-noun false positives (`top`/`bottom`/`back` as nouns) still win outright; the tune only cut the margin from 0.168 to 0.018. So "to config?" is really "the fixed filename confidence must drop below achievable CLIP orientation (≤0.582) *or* be gated on token validity," and the just-landed 0.60 clears neither bar. Improved from the landed config against the T-5000 measurement, not a guess.
- **Export Todo 6 — `upscale-manifest.json` `KoTooSmall`** ([lib/Export/jbtodo.md](jb/src/core/lib/Export/jbtodo.md)): the T-4980 review refines yesterday's answer. On full-bleed images the KO cannot be assumed to key off the *promoted* subject box — the T-4980 review found that box is overwritten downstream by `DetectSalientBoundingBox` in `PreprocessAsync`, so on those images the size gate still measures the salient box. The manifest's `KoTooSmall` reason string and any "% upscaled" denominator must be reproduced against whichever box `PreprocessAsync` actually emits, not the promoted box a reader would assume. Improved from the landed T-4980 review, not a guess.
- The rest stay genuinely blocked: Match items 1/4 still wait on CiMini Bracket-3/Bracket-4 fixtures (root `jbtodo.md`), and CiMini's Full golden is separately red (T-4980); Export Todo 4's 7 `Tx_*.cs` still don't write their own param values; Transform HeadCutter Algorithm A still needs measured crown-offset constants; Generate stays FROZEN behind the ComfyUI backend.
