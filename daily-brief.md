# Daily Brief

##### Changed
- **T-4970 closed and archived (03/08); off the board.** Its last open item — re-measure at the shipped bars (`hero-orientation` 0.33 / `head-visible` 0.25 / `body-visible` 0.10, `OrientationConfidence` 0.60) — was run twice from fresh processes and gave **byte-identical** JSON, so the harness itself is verified reliable. Numbers: phenotype coverage **37.2% (32/86)** on SPACINI29, 28.6% (4/14) on CiMini. `back-on-model-partial` is the single largest bucket (18 of 32) — it fires on the exact case it was added for. Ordering moved with it: **27/86 now land in a real phenotype-driven det slot** (was 0, all-overflow). Whether the 32 assignments are *correct* was deliberately not scored — that needs the hand-labelled `dataset notes.md` ground truth.
- **New tracked dataset `test/JBComplete/` committed** (93 images + `Brackets-Complete.xlsx`, commit `39bd0ad`) — the first tracked multi-bracket/multi-phenotype batch besides CiMini (the rest of `test/datasets/` is gitignored). The filenames deliberately target both open dataset-gap todos: `4471-2290`/`4471-2340` (two separately-ambiguous numbers), `8712345678901` (long number hiding inside a bigger barcode), `foldercontainsID99984905`/`26182-Denim-801` (meaningless filename in a meaningful folder), `TH12_0N3_M47CH32_70_R3D_36R37` (picture-only Bracket-4), plus `BW001_c`/`FW001_b…f` packshot codes and `triggered_ghost-front/back`.
- **`SubjectDetectionResult.cs` moved out of `Prism.Core.Contracts` back into the Matching project** (`8d92c82`, csproj Content/Compile adjusted). Corrects an earlier misplacement; no pipeline behaviour change.

##### Next steps
- Score the 32 T-4970 phenotype assignments for correctness against SPACINI29 `dataset notes.md` — coverage is measured now, accuracy is not, and that's the M11 acceptance bar.
- Add a direct `ruleSet.Assign` positive+negative test for `back-on-model-partial` — T-4970's one non-blocking warning: only the 20→21 count is asserted today.
- Run `-Mode Match` over `test/JBComplete` before treating it as bracket coverage — confirm `4471-2290` / `8712345678901` / folder-name / obfuscated cases actually reach their intended brackets (filenames assert intent, not behaviour).
- Land T-4955 + T-4990 + T-5000 — all 13 surviving T-4970 mislabels are upstream (9 CLIP orientation, 4 detector under-count), none fixable in `ImageRoles.json`.
- Still open, unchanged: rework T-4980 so the full-frame box lands where `PreprocessAsync` reads it (today it writes a box nothing consumes) and make `dotnet test` read the goldens; fix T-5010's 6 red Transform fixtures before its verdict; T-4942 GPU-test serialization; T-4960 alpha-box preference; T-4000 analyzer-calibration split.

##### Todo updates
- **Classify item 2 — phenotype production validation** ([Classify/jbtodo.md](jb/src/core/Services/Matching/Classify/jbtodo.md)): the Answer's "next step is the light first pass — T-4970, blocked until the upscale epic completes" is now landed history. T-4900 (upscale epic) closed 30/07 and T-4970 closed 03/08, and the pass produced the real distribution — 37.2% coverage, `back-on-model-partial` the largest bucket at 18, 27/86 gaining a real det slot. So the concrete next step shifts from "run the light pass" to "score those 32 for correctness against ground truth" — the reliability question is answered, the accuracy question is now the blocker. Improved from the archived T-4970 close, not a guess.
- **Root — CiMini per-bracket coverage** ([jbtodo.md](jbtodo.md)): the source images this todo asked to commission now largely exist in `test/JBComplete/` — `4471-2290`/`4471-2340` (two-ambiguous-numbers), `8712345678901` (long-number-inside-barcode), `foldercontainsID99984905`/`26182-Denim-801` (folder-name borrow), `TH12_0N3…` (picture-only Bracket-4), plus CARDIGAN/Pareo sibling pairs. Two caveats keep it open: it's a separate batch with its own `Brackets-Complete.xlsx`, so it does **not** recapture CiMini's committed goldens, and each case still needs a `-Mode Match` run to confirm it reaches the intended bracket rather than resolving earlier. Improved from the committed dataset listing, not a guess.
- **Root — purpose-built phenotype dataset (from T-4970)** ([jbtodo.md](jbtodo.md)): `test/JBComplete/` carries the CiGolden family codes (`BW001_c`, `FW001_b…f`, `bottom/side/top-packshot`) and `triggered_ghost-front/back`, so the images partly exist. But it does not yet satisfy the todo's own two constraints: many filenames leak orientation keywords (`front`/`back`/`detail`/`side`) that `Analyzer_FilenameEvidence` reads straight into `hero-orientation`, so it can't isolate CLIP+analyzer correctness the way the deliberately keyword-free CiGolden spec requires; and there's no per-image `expected-phenotype.json` ground-truth file, so it measures coverage/matching, not phenotype correctness. Improved from the committed dataset listing against the todo's stated rules, not a guess.
- The rest stay genuinely blocked: Match items 1/4 still wait on validation runs of the new Bracket-3/Bracket-4 cases; Export Todo 4's 7 `Tx_*.cs` still don't write their own param values; Transform HeadCutter Algorithm A still needs measured crown-offset constants; Generate stays FROZEN behind the ComfyUI backend.
