# Daily Brief

##### Changed
- **Repo-wide K&R brace reformat, now CI-enforced (d365236 + 87c6ed4)** — the material delta this pass. 250 files reformatted (~2996 ins / 5061 del, net −2065; the imbalance is expected — collapsing each Allman `{` off its own line onto the previous line removes lines). No logic touched; every C# source + test file moved to opening-brace-on-same-line to match the CLAUDE.md house rule. A new CI step, **Enforce K&R brace style**, runs `dotnet format whitespace jb/src/PRISM.sln --verify-no-changes` and fails the build (non-zero exit, no rewrite) on any Allman brace that creeps back. SA1500 stays permanently suppressed; this is a formatter gate, not an analyzer warning. Net effect: the two "pre-existing Allman brace nits" the T-3800 reviewer flagged are now structurally impossible going forward.
- **Two Match todos closed (6c60450)** — exactly the two the last brief's Next steps recommended. Substring-rescue perf (measured, "not worth an n-gram index") and the fuzzy-fallback 4-layer future-work note (decision: do not build speculatively) both had no code and no validation dependency, so both `/todo-finish`'d. Decisions moved into `jb/docs/PRISM-match.md` (new "Future Work — StringMatcher toward semantic/multilingual matching" section preserving each layer's prerequisite cost; substring-rescue line at `:66`). `Match/jbtodo.md` now holds only items 1 (edit-distance gap) and 4 (`totalImageTokens`).
- **T-3800 reviewer Approve recorded (4010604)** — verdict on the `e2e1f84`+`f40beed` diff: items 1/2/4 correctly implemented, config-driven with no shadow defaults, tested for hits and misses, build clean + 202/202 Matching tests green. The P1 review gate is now satisfied, so T-3800 is `/ticket-finish`-eligible the moment items 1 and 4 validate. With items 2/3 closed, its **sole remaining blocker is the CiMini expansion** — nothing else.
- Minor: two follow-up board corrections (c358136, ea2be33) tightened the 07-24 sync wording; `.vscode/settings.json` trimmed 328 lines of folder-color housekeeping (9bb462f). No config, model-asset, or pipeline-stage behavior changed.

##### Next steps
- Build the CiMini expansion (root `jbtodo.md`) — now the single highest-leverage task: it's the only thing standing between T-3800 and Done, and the todo already enumerates every per-bracket case to add (grey/gray Bracket-3 fuzzy case for item 1, a Bracket-4-reaching image for item 4). Follow the CiMini README recapture procedure.
- Confirm the new K&R gate went green on main — if the reformat missed any file, `dotnet format whitespace --verify-no-changes` will now fail CI; catch it before it blocks the next PR.
- T-4000: start `Analyzer_FacePose` (6 features, unblocks the most on-model phenotypes, milestone-independent — startable anytime).
- Board's only "Ready" work stays T-3800 (Match, code+review done, awaiting CiMini validation) and T-4000 (Analyzer TOC); T-2600 stays Blocked behind T-4000's Analyzer stubs + a BypassPhenotypes decision.

##### Todo updates
- None — nothing improvable without guessing. The two Match todos that were improvable from existing data (substring-rescue perf, fuzzy-fallback note) were closed this pass via `/todo-finish`; the rest are genuinely blocked, not under-answered — Match items 1/4 wait on a CiMini validation run, the root CiMini gap and Transform HeadCutter Algorithm A both need data that doesn't exist in the repo (real per-bracket fixtures; measured anatomical crown-offset constants), and Classify/Generate stay FROZEN.
