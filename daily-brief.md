# Daily Brief

##### Changed
- The batch the last brief queued up has landed. **T-2820** and **T-2830** (the two det-slot tickets — non-deterministic tied-family slots, and det-numbering starting at det8 instead of det0) are closed and archived with `Approve` reviews (e32bb4a). The "run 5+ CiMini before spending effort on T-2820" recommendation is now spent.
- **T-3500** closed (a8e6987): the Import→Match in-process decode fusion shipped — `MatchingService.PrepareLambda`'s second `Image.Load` of `NormalizedJpgPath` is removed when Import and Match share a process, with a measured close-out on SPACINI29 (e3c5fa7). Its source root-`jbtodo.md` item (the Import→Match handoff) closed too (cd4bc59), so the last brief's "measure decode share + peak RSS" step was answered, not deferred.
- **T-3600** closed (3aec494): Matching's HTTP contract no longer *silently* assumes a shared filesystem — the Ingress+Matching+Export co-deployment (one physical system sharing the job temp folder / artifact bus) is now the documented deliberate contract, and `AGENTFEEDBACK.md` records `NormalizedJpgPath`-as-local-path as intentional. Only Transform/Generate/Upscale may split out.
- **T-3700** closed (0aabc6d): assembly / namespace / solution names realigned with the Services/ restructure. Consequence for the docs-vocab todo — the `Prism.Core.Images.*` assembly-name drift it lists as "still stale, T-3700" is gone (grep-clean across `jb/docs/`); only the `core/Images/*` *path* drift remains.
- **T-4100** closed (0aabc6d, `Approve`): the GPU-reports-CPU health investigation surfaced YOLO running CPU-only always + ONNX Runtime version skew (CLIP 1.20.1 vs Upscale 1.24.4), which spun off new ticket **T-4110** (a8133df) to unify ONNX EP policy across CLIP/YOLO/Upscale under one version + one session factory.
- **T-3300** moved from Ready to in-progress: step 0 landed (1c4ea63) — `Prism.Core.Tests` retargeted net9.0→net10.0 to align with Api/ServiceHost and unblock `WebApplicationFactory` for the not-yet-written HTTP-roundtrip tests.

##### Next steps
- **T-4110:** start with the concrete bug — `YoloDetector.cs:65` appends no execution provider, so YOLO is CPU-only even on a GPU box. Route it (plus CLIP/Upscale) through the one shared `OnnxSessionFactory`, then re-run CiMini golden 5× after the version-pin bump, since changing CLIP's runtime can shift FP results and reopen T-2820's determinism.
- **T-3300:** with net10 aligned, add the first real-HTTP roundtrip test for one `Http*Service` against `Prism.ServiceHost`/`WebApplicationFactory` before doing the per-service `.csproj` split — the split only pays off once a distributed run is proven.
- **Docs-vocabulary sweep:** now fully unblocked (T-3700 cleared the assembly-name half). Start from the ~25 confirmed `core/Images/*` path instances across 8 doc files (inventory below), and skip the deliberate "deleted, do not reintroduce" type notes in `PRISM-models.md`/`PRISM-pipeline-core.md` — those are correct, not drift.
- **T-3800 Match items:** each still needs its own measurement/decision — cheapest first is a `Stopwatch` around `TryMatchBySubstringRescue` on a CiMini/full batch to settle whether the brute-force digit-index scan is a real hotspot before building an n-gram index.

##### Todo updates
- **Docs-vocabulary sweep** — improved its OPEN answer with a concrete `core/Images/*` path-drift inventory read straight off `grep`: `GLOSSARY.md` (2: MCFG→`Images/Match/`, DOR→`Images/Order/`), `PRISM-transform-generate.md` (7), `PRISM-classify.md` (4), `PRISM-match.md` (4), `meta/currentMatching.md` (4), `meta/MatchingDeltaVsTarget.md` (2), `ImageNGP/ImageFeatures.md` (1), `ideas-on-NGP.md` (1) — all `Images/*`→`Services/*`. Also: the todo's `Prism.Core.Images.*` "still stale, T-3700" note is now resolvable — T-3700 landed and those names grep-clean in docs; and the `ImageTransformationResult`/`PrismConfigLocator`/`ConfigCache` mentions in `PRISM-models.md`/`PRISM-pipeline-core.md` are intentional "deleted" notes, not sweep targets. Why: every instance is a real path pulled from an existing file, no guessing: [jbtodo.md](jbtodo.md).
