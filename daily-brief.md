# Daily Brief

##### Changed
- **T-4700 + T-4710 both Done and archived (fe9ac38, fd894aa, closed 43dd80f)** — the "simplify by subtraction, then re-expand piecemeal" pass the board has been pointing at, now landed with reviewer Approve on both. T-4700 deleted the 10 empty-body analyzer stubs and everything mathematically downstream of them: `ImageNGP.json` went 60→37 features and 26→20 phenotypes, and the `background-type=STUDIO` enum value was dropped. The mechanism is worth restating once because it drove the whole cut — `PhenotypeRuleSet` never counts an `UNKNOWN` feature as satisfying a `required` condition, so any phenotype hard-gated on a stub-only feature was unreachable, not merely uncalibrated; 6 phenotypes were dead-on-arrival, cascading into 13 of 19 `DetOrderRules.json` tables carrying an inert det-slot. Those slots became `[]` (kept, not deleted) to preserve overflow slot numbering. T-4710 then collapsed `ProductTypeMap.json`/`DetOrderRules.json` from 19 product types to 5 (`default` + `topwear`, `bottomwear`, `footwear`, `bags-accessories`); the other 13 fall back to `default`. `topwear`/`bottomwear` tables are byte-for-byte the old `clothing-tops`/`clothing-bottoms` content under new keys (verified against git history). It also surfaced `OrderEvidence.WinningPhenotype` on the export manifest — computed by `ImageOrderer` all along but never exported — so a consumer can read *why* an image landed in a det slot instead of inferring it from position. Full suite 417/417 after both.
- **T-4800 subject-isolation epic added to the board (3a2630d)** — a new 9-ticket body of work (T-4805–T-4870, of which T-4840 is already done) folding in the removed `TRANSFORM-SUBJECT-ISOLATION-NOTE.md` plus the vendored classical-CV prototype `jb/docs/reference/process_images.py`. Goal: give Transform a real subject mask/box (shadow- and background-excluded) produced *upstream* and consumed as pure geometry+fill, keeping ONNX out of Transform so transforms stay deterministic. Design lives in `Transform/Engine/jbtodo.md`. Wave 0 (T-4805/4810/4820) is P4-critical-architecture and blocks everything else. The top-priority finding is latent-not-live: `Tx_CenterAndStretch.Process` ignores its `lambda` parameter and always crops to full-image bounds, so a future per-image webservice on `Process` would diverge from the pipeline `Transform(lambda)` path and ignore the persisted SubjectBox — T-4805 unifies the two entry points before that matters.

##### Next steps
- Start T-4805 (unify Transform/Process entry points) — Wave-0 P4 and the epic's own top priority; it's pure de-duplication of two paths (no new transform behavior) and unblocks T-4810/T-4850's persisted-box consumption.
- Then T-4810 (persisted SubjectMask/SubjectBox contract) + T-4820 (thread `product-color`/`background-type`/`background-color`/`product-type-label` + FamilyIDRecord into Transform) — the other two Wave-0 tickets; both are contract/plumbing only, no detector or seeding logic yet.
- Board action from the epic: thaw T-2600 (user-directed) so a future segmentation producer (SAM3 / yolo26s-seg) can sit behind the T-4810 contract.
- T-3800's only remaining blocker is unchanged — the CiMini expansion (root `jbtodo.md`) for empirical validation of items 1 & 4; the todo already enumerates every per-bracket case to add.
- T-4000 item 1 (11 real analyzers, calibration open) is the sole live analyzer backlog now; `Analyzer_ProductType` is the natural first pick since T-4710 just reshaped its output vocabulary to 5 slugs.

##### Todo updates
- **Classify item 2 — phenotype production validation** ([Classify/jbtodo.md](jb/src/core/Services/Matching/Classify/jbtodo.md)): the answer's near-term step ("a lighter first-pass check … defined as part of the follow-up ticket that collapses DetOrderRules.json to 5 product types") is now **unblocked** — that ticket is T-4710, Done and archived 2026-07-27. The prerequisite it named is satisfied, and T-4710 also exposed `OrderEvidence.WinningPhenotype` on the export manifest, which is exactly the per-image "which phenotype won, and why this det slot" signal a before/after `prism-evidence-report` diff on the standard dataset needs to read. So the lighter validation pass is now actionable data-wise (the full 200-images/phenotype protocol stays FROZEN, unchanged). Improved from a landed commit, not a guess.
- The other open todos stay genuinely blocked, not under-answered: Match items 1/4 still wait on the same CiMini Bracket-3/Bracket-4 fixtures; the root CiMini gap and Transform HeadCutter Algorithm A both need data absent from the repo (real per-bracket goldens; measured anatomical crown-offset constants); Classify item 1 was already resolved by T-4700; Generate stays FROZEN behind the ComfyUI backend.
