# Daily Brief

## Check overnight

- **`Wait-PrismResult` crash in `test/test-scripts/PrismJobRunner.psm1`**: `[System.Text.Encoding]::UTF8.GetString($response.Content)` throws `Cannot convert argument "bytes" ... to type "System.Byte[]"` on every completed JSON-format job — `$response.Content` is already a `string` on this PowerShell version, not a `byte[]`. Blocks a clean `Write-PrismLogLine` entry for every dataset that actually completes, and balloons the log with the same manifest dumped repeatedly before the script limps on to the next folder. Needs a real fix (branch on the response type, or just use the string directly) before `Run_MatchingOnly.ps1` / `Run_All.ps1` results can be trusted from the log file alone.
- **AUTOMAT2 and HEROAUT2 both came back 100% KO with `NO_FAMILIES`** ("No FamilyID records were parsed from the Excel input") — 0/1054 and 0/2612 respectively. INPUTMA23 in the same run matched fine (591/921 Ok), so this looks dataset-specific rather than a global regression, but worth confirming against those two datasets' Excel files directly.
- **HEROAUT3 submission failed** with `SUBMIT_ERROR: Exception calling "GetResult" ... "Error while copying content to a stream."` after only 21s — didn't even get a JobID. Check if reproducible or a one-off.
- **Full sweep incomplete**: only AUTOMAT2, HEROAUT2, HEROAUT3, INPUTMA23 finished; INPUTMA24 was interrupted mid-result (job had completed server-side but the script never got past it before I stopped everything for the night); INPUTMA25, INPUTMA27, MMERO26, SPACINI29, TinyTest never ran.